<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orbit All-in-One Poker & Stream Manager // Alpha Unit Access</title>
    <link data-font="orbit-elite"
        href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=Share+Tech+Mono&display=swap"
        rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="orbit_icon.ico">

    <!-- ORBIT CONFIG: API Keys Loader -->
    <script src="config.js"></script>

    <style>
        :root {
            --font-display: 'Orbitron', sans-serif;
            --font-tech: 'Share Tech Mono', monospace;
            --bg-grid-color: rgba(0, 242, 255, 0.03);
            --bg-dark: #02050a;
            --bg-deep: #000000;
            --card-bg: rgba(10, 15, 25, 0.85);
            --cyan-neon: #00f2ff;
            --cyan-glow: rgba(0, 242, 255, 0.4);
            --magenta-neon: #ff00ff;
            --magenta-glow: rgba(255, 0, 255, 0.3);
            --text-main: #f8fafc;
            --text-dim: #94a3b8;
            --border-glow: rgba(0, 242, 255, 0.2);
            --poker-red: #ff3e3e;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: var(--font-tech) !important;
            background-color: var(--bg-dark);
            background-size: 40px 40px;
            background-image:
                linear-gradient(to right, var(--bg-grid-color) 1px, transparent 1px),
                linear-gradient(to bottom, var(--bg-grid-color) 1px, transparent 1px),
                radial-gradient(circle at 50% 50%, #050a14 0%, #000 100%);
            color: var(--text-main);
            overflow-x: hidden;
            position: relative;
        }

        h1,
        h2,
        h3,
        .logo-text,
        .hero-badge,
        .nav-link,
        .btn,
        .tag,
        .hud-value,
        .hud-label,
        .faq-item h3 {
            font-family: var(--font-display) !important;
            letter-spacing: 1px;
        }

        .live-status {
            position: fixed;
            top: 30px;
            right: 40px;
            bottom: auto;
            left: auto;
            background: rgba(0, 10, 20, 0.95);
            border: 1px solid var(--cyan-neon);
            padding: 10px 25px;
            border-radius: 4px;
            z-index: 9999;
            box-shadow: 0 0 20px rgba(0, 242, 255, 0.15);
            display: flex;
            align-items: center;
            gap: 15px;
            backdrop-filter: blur(10px);
        }

        /* --- Custom Scrollbar --- */
        ::-webkit-scrollbar {
            width: 6px;
        }

        ::-webkit-scrollbar-track {
            background: var(--bg-dark);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--cyan-neon);
            border-radius: 10px;
        }

        /* --- Navbar --- */
        nav {
            padding: 15px 60px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            background: rgba(2, 5, 10, 0.9);
            backdrop-filter: blur(25px);
            border-bottom: 1px solid rgba(0, 242, 255, 0.15);
        }

        .logo-box {
            display: flex;
            align-items: center;
            gap: 15px;
            text-decoration: none;
        }

        .logo-img {
            width: 50px;
            height: 50px;
            filter: drop-shadow(0 0 15px var(--cyan-neon));
            animation: spin-slow 20s linear infinite;
            transition: 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .logo-box:hover .logo-img {
            transform: rotate(360deg) scale(1.1);
            border-color: var(--cyan-neon);
            box-shadow: 0 0 30px var(--cyan-neon), 0 0 60px var(--cyan-glow);
            filter: brightness(1.2);
        }

        .logo-text {
            font-family: 'Outfit', sans-serif;
            font-weight: 900;
            letter-spacing: 3px;
            color: #fff;
            font-size: 20px;
            text-transform: uppercase;
        }

        .nav-links {
            display: flex;
            gap: 30px;
            align-items: center;
        }

        .nav-link {
            text-decoration: none;
            color: var(--text-dim);
            font-size: 12px;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            transition: 0.3s;
        }

        .nav-link:hover {
            color: var(--cyan-neon);
            text-shadow: 0 0 10px var(--cyan-glow);
        }

        /* --- Hero Section --- */
        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 140px 20px 80px;
            position: relative;
        }

        .hero-badge {
            background: rgba(0, 242, 255, 0.1);
            color: var(--cyan-neon);
            padding: 8px 22px;
            border-radius: 50px;
            font-size: 11px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 4px;
            margin-bottom: 35px;
            border: 1px solid var(--cyan-neon);
            box-shadow: 0 0 25px rgba(0, 242, 255, 0.2);
            animation: glowPulse 3s infinite;
        }

        @keyframes glowPulse {

            0%,
            100% {
                box-shadow: 0 0 15px rgba(0, 242, 255, 0.2);
                border-color: var(--cyan-neon);
            }

            50% {
                box-shadow: 0 0 35px rgba(255, 0, 255, 0.4);
                border-color: var(--magenta-neon);
            }
        }

        .hero h1 {
            font-family: 'Outfit', sans-serif;
            font-size: clamp(40px, 12vw, 110px);
            font-weight: 950;
            line-height: 0.8;
            margin-bottom: 40px;
            color: #fff;
            letter-spacing: -4px;
            text-transform: uppercase;
            perspective: 1000px;
        }

        .hero h1 span {
            display: block;
            background: linear-gradient(to right, var(--cyan-neon), var(--magenta-neon));
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            filter: drop-shadow(0 0 20px rgba(0, 242, 255, 0.3));
        }

        .hero p {
            font-size: clamp(16px, 2.5vw, 24px);
            color: var(--text-dim);
            max-width: 900px;
            margin-bottom: 60px;
            font-weight: 300;
        }

        .hero p b {
            color: var(--cyan-neon);
            font-weight: 800;
        }

        .cta-container {
            display: flex;
            gap: 30px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .btn {
            padding: 22px 50px;
            border-radius: 12px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 2px;
            transition: 0.5s cubic-bezier(0.2, 1, 0.3, 1);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 15px;
            font-size: 15px;
            position: relative;
            overflow: hidden;
        }

        .btn-orbit {
            background: var(--cyan-neon);
            color: var(--bg-dark);
            border: 2px solid var(--cyan-neon);
            box-shadow: 0 10px 40px rgba(0, 242, 255, 0.3);
        }

        .btn-orbit:hover {
            transform: translateY(-10px) rotateX(10deg);
            box-shadow: 0 20px 60px var(--cyan-glow);
            background: #fff;
            border-color: #fff;
        }

        .btn-telegram {
            background: rgba(255, 0, 255, 0.1);
            color: #fff;
            border: 2px solid var(--magenta-neon);
        }

        .btn-telegram:hover {
            background: var(--magenta-neon);
            transform: translateY(-10px) rotateX(-10deg);
            box-shadow: 0 20px 60px var(--magenta-glow);
        }

        /* --- Multimodel AI Section --- */
        .ai-section {
            padding: 120px 60px;
            background: rgba(0, 0, 0, 0.4);
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .ai-container {
            display: flex;
            align-items: center;
            gap: 100px;
            max-width: 1400px;
            margin: 0 auto;
        }

        .ai-text {
            flex: 1.2;
        }

        .ai-visual {
            flex: 0.8;
        }

        .ai-card {
            background: linear-gradient(145deg, #0a0a1a, #050505);
            padding: 40px;
            border-radius: 25px;
            border: 1px solid var(--cyan-neon);
            box-shadow: 0 0 50px rgba(0, 242, 255, 0.1);
            position: relative;
        }

        .ai-status-bar {
            background: #000;
            border-radius: 10px;
            padding: 15px;
            font-family: 'JetBrains Mono', monospace;
            font-size: 13px;
            color: #0f0;
            margin-bottom: 20px;
        }

        .latency-badge {
            background: var(--magenta-neon);
            color: #fff;
            padding: 4px 10px;
            border-radius: 5px;
            font-size: 11px;
            font-weight: 900;
        }

        /* --- Toolbox Categorized --- */
        .toolbox {
            padding: 140px 60px;
            background: var(--bg-deep);
        }

        .category-group {
            margin-bottom: 100px;
        }

        .category-title {
            font-family: 'Outfit', sans-serif;
            font-size: 30px;
            color: var(--cyan-neon);
            margin-bottom: 40px;
            display: flex;
            align-items: center;
            gap: 20px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .category-title::after {
            content: '';
            height: 2px;
            flex: 1;
            background: linear-gradient(to right, var(--cyan-neon), transparent);
        }

        .tool-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 30px;
        }

        .tool-card {
            background: rgba(255, 255, 255, 0.02);
            padding: 45px;
            border-radius: 25px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            transition: 0.5s;
            position: relative;
            overflow: hidden;
        }

        .tool-card:hover {
            transform: translateY(-15px);
            background: rgba(255, 255, 255, 0.04);
            border-color: var(--magenta-neon);
            box-shadow: 0 20px 50px rgba(255, 0, 255, 0.1);
        }

        .tool-icon {
            font-size: 45px;
            margin-bottom: 30px;
            display: block;
            filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.3));
        }

        .tool-card h3 {
            font-family: 'Outfit', sans-serif;
            font-size: 24px;
            color: #fff;
            margin-bottom: 15px;
        }

        .tool-card p {
            color: var(--text-dim);
            font-size: 15px;
            line-height: 1.7;
        }

        .tool-card .tag {
            display: inline-block;
            margin-top: 20px;
            font-size: 10px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--cyan-neon);
            padding: 4px 10px;
            border: 1px solid var(--cyan-neon);
            border-radius: 4px;
        }

        /* --- Footer --- */
        footer {
            padding: 100px 60px;
            background: #000;
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 60px;
            max-width: 1200px;
            margin: 0 auto 80px;
            text-align: left;
        }

        .footer-col h4 {
            color: #fff;
            margin-bottom: 25px;
            font-family: 'Outfit', sans-serif;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .footer-col a {
            display: block;
            color: var(--text-dim);
            text-decoration: none;
            margin-bottom: 15px;
            font-size: 14px;
            transition: 0.3s;
        }

        .footer-col a:hover {
            color: var(--cyan-neon);
        }

        .copyright {
            color: var(--text-dim);
            font-size: 11px;
            letter-spacing: 6px;
            opacity: 0.5;
            text-transform: uppercase;
        }

        /* --- Live Status --- */
        .live-status {
            position: fixed;
            bottom: 40px;
            left: 40px;
            background: rgba(2, 5, 10, 0.95);
            padding: 15px 25px;
            border-radius: 50px;
            border: 1px solid var(--cyan-neon);
            display: flex;
            align-items: center;
            gap: 15px;
            z-index: 2000;
            box-shadow: 0 0 30px var(--cyan-glow);
            backdrop-filter: blur(10px);
        }

        .pulse-dot {
            width: 12px;
            height: 12px;
            background: #00ff00;
            border-radius: 50%;
            box-shadow: 0 0 15px #00ff00;
            animation: blink 2s infinite;
        }

        @keyframes blink {

            0%,
            100% {
                opacity: 1;
                transform: scale(1);
            }

            50% {
                opacity: 0.4;
                transform: scale(0.8);
            }
        }

        @media (max-width: 1024px) {
            .ai-container {
                flex-direction: column;
                text-align: center;
                gap: 60px;
            }

            nav {
                padding: 15px 30px;
            }
        }

        /* --- Philosophy & Glitch Section --- */
        .philosophy-section {
            padding: 140px 60px;
            background: linear-gradient(to bottom, transparent, rgba(255, 0, 255, 0.03), transparent);
            position: relative;
        }

        .glitch-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 40px;
            max-width: 1400px;
            margin: 0 auto;
        }

        .glitch-card {
            background: rgba(10, 15, 25, 0.6);
            border: 1px solid rgba(0, 242, 255, 0.2);
            padding: 50px;
            border-radius: 20px;
            position: relative;
            overflow: hidden;
            transition: 0.4s;
            cursor: pointer;
        }

        .glitch-card::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, transparent, rgba(0, 242, 255, 0.05), transparent);
            transform: translateX(-100%);
            transition: 0.6s;
        }

        .glitch-card:hover::before {
            transform: translateX(100%);
        }

        .glitch-card:hover {
            border-color: var(--cyan-neon);
            transform: translateY(-10px);
            background: rgba(10, 15, 25, 0.9);
            box-shadow: 0 10px 40px rgba(0, 242, 255, 0.2);
        }

        .glitch-title {
            font-family: 'Outfit', sans-serif;
            font-size: 24px;
            font-weight: 900;
            color: var(--cyan-neon);
            margin-bottom: 25px;
            text-transform: uppercase;
            letter-spacing: 2px;
            position: relative;
        }

        /* Real Glitch Effect on Hover */
        .glitch-card:hover .glitch-title {
            animation: glitch-text 0.3s infinite;
        }

        @keyframes glitch-text {
            0% {
                transform: translate(0);
                text-shadow: -2px 0 var(--magenta-neon), 2px 0 var(--cyan-neon);
            }

            20% {
                transform: translate(-2px, 2px);
            }

            40% {
                transform: translate(-2px, -2px);
                text-shadow: 2px 0 var(--magenta-neon), -2px 0 var(--cyan-neon);
            }

            60% {
                transform: translate(2px, 2px);
            }

            80% {
                transform: translate(2px, -2px);
                text-shadow: -2px 0 var(--magenta-neon), 2px 0 var(--cyan-neon);
            }

            100% {
                transform: translate(0);
            }
        }

        .glitch-card p {
            font-size: 16px;
            color: var(--text-dim);
            line-height: 1.8;
            font-weight: 400;
        }

        .glitch-line {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(to right, var(--cyan-neon), var(--magenta-neon));
            transform: scaleX(0);
            transform-origin: left;
            transition: 0.5s cubic-bezier(0.2, 1, 0.3, 1);
        }

        .glitch-card:hover .glitch-line {
            transform: scaleX(1);
        }

        .copilot-banner {
            background: rgba(0, 242, 255, 0.02);
            border: 1px solid rgba(0, 242, 255, 0.2);
            padding: 80px 40px;
            border-radius: 40px;
            max-width: 1100px;
            margin: 0 auto;
            position: relative;
            backdrop-filter: blur(20px);
            box-shadow: inset 0 0 50px rgba(0, 242, 255, 0.05);
        }

        .copilot-tag {
            font-family: 'JetBrains Mono', monospace;
            color: var(--magenta-neon);
            font-size: 12px;
            letter-spacing: 4px;
            text-transform: uppercase;
            margin-bottom: 20px;
            display: block;
        }

        @media (max-width: 768px) {
            .philosophy-section {
                padding: 80px 20px;
            }

            .ai-section {
                padding: 80px 20px;
            }

            nav {
                padding: 15px 20px;
            }
        }

        /* --- BRANDING: SPLASH & WATERMARK --- */
        #orbit-splash {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: radial-gradient(circle at center, #0a1220 0%, #02050a 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 100000;
            transition: opacity 1.5s cubic-bezier(0.4, 0, 0.2, 1);
            pointer-events: all;
        }

        #orbit-splash.fade-out {
            opacity: 0;
            pointer-events: none;
        }

        .splash-content {
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 30px;
        }

        .splash-logo-container {
            position: relative;
            width: min(60vh, 80vw);
            height: min(60vh, 80vw);
        }

        .splash-logo {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            border: 4px solid var(--cyan-neon);
            box-shadow: 0 0 30px var(--cyan-glow);
            animation: splash-pulse-landing 2s infinite cubic-bezier(0.4, 0, 0.2, 1);
        }

        .splash-scan-line {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: var(--cyan-neon);
            box-shadow: 0 0 15px var(--cyan-neon);
            animation: scan-move 2.5s infinite linear;
            opacity: 0.7;
        }

        @keyframes splash-pulse-landing {

            0%,
            100% {
                transform: scale(1);
                box-shadow: 0 0 30px var(--cyan-glow);
            }

            50% {
                transform: scale(1.08);
                box-shadow: 0 0 60px var(--cyan-neon);
            }
        }

        @keyframes scan-move {
            0% {
                top: 0;
                opacity: 0;
            }

            10% {
                opacity: 1;
            }

            90% {
                opacity: 1;
            }

            100% {
                top: 100%;
                opacity: 0;
            }
        }

        .splash-text-box {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .splash-title {
            font-family: 'Outfit', sans-serif;
            color: #fff;
            font-size: 24px;
            font-weight: 950;
            letter-spacing: 12px;
            text-transform: uppercase;
            text-shadow: 0 0 20px rgba(0, 242, 255, 0.5);
            margin-left: 12px;
        }

        .splash-subtitle {
            font-family: 'JetBrains Mono', monospace;
            color: var(--cyan-neon);
            font-size: 11px;
            letter-spacing: 5px;
            opacity: 0.8;
            text-transform: uppercase;
        }

        .splash-loader-bar {
            width: 280px;
            height: 2px;
            background: rgba(255, 255, 255, 0.05);
            position: relative;
            overflow: hidden;
            border-radius: 2px;
            margin-top: 10px;
        }

        .splash-progress {
            position: absolute;
            top: 0;
            left: 0;
            height: 100%;
            width: 0;
            background: var(--cyan-neon);
            box-shadow: 0 0 15px var(--cyan-neon);
            animation: none;
        }

        @keyframes progress-load-landing {
            0% {
                width: 0;
            }

            20% {
                width: 30%;
            }

            50% {
                width: 45%;
            }

            80% {
                width: 95%;
            }

            100% {
                width: 100%;
            }
        }

        .splash-progress.scanning {
            animation: progress-load-landing 4s forwards cubic-bezier(0.4, 0, 0.2, 1);
        }

        #init-scanner-btn {
            background: linear-gradient(135deg, rgba(0, 242, 255, 0.1), rgba(0, 0, 0, 0.8));
            border: 1px solid var(--cyan-neon);
            color: var(--cyan-neon);
            padding: 12px 25px;
            font-family: 'Outfit', sans-serif;
            font-weight: 800;
            letter-spacing: 2px;
            text-transform: uppercase;
            font-size: 14px;
            cursor: pointer;
            margin-bottom: 20px;
            transition: all 0.3s;
            box-shadow: 0 0 15px rgba(0, 242, 255, 0.1);
        }

        #init-scanner-btn:hover {
            background: rgba(0, 242, 255, 0.2);
            box-shadow: 0 0 30px var(--cyan-glow);
            transform: scale(1.02);
            text-shadow: 0 0 8px var(--cyan-neon);
        }

        .orbit-watermark {
            position: fixed;
            bottom: 25px;
            right: 25px;
            width: 80px;
            height: 80px;
            opacity: 0.15;
            z-index: 100;
            pointer-events: none;
            transition: opacity 0.5s ease;
        }

        .orbit-watermark img {
            width: 100%;
            height: 100%;
            filter: grayscale(1) brightness(2);
            mix-blend-mode: overlay;
        }

        /* --- Audio Control UI --- */
        .audio-toggle {
            display: flex;
            align-items: center;
            gap: 10px;
            padding-left: 15px;
            border-left: 1px solid rgba(255, 255, 255, 0.1);
            cursor: pointer;
            transition: 0.3s;
        }

        .audio-toggle:hover {
            filter: brightness(1.2);
        }

        .audio-bars {
            display: flex;
            align-items: flex-end;
            gap: 2px;
            height: 12px;
        }

        .audio-bars .bar {
            width: 2px;
            background: var(--text-dim);
            height: 4px;
            transition: height 0.2s ease;
        }

        .audio-toggle.active .bar {
            background: var(--cyan-neon);
            animation: audio-bounce 0.8s infinite ease-in-out;
        }

        .audio-toggle.active .bar:nth-child(2) {
            animation-delay: 0.2s;
        }

        .audio-toggle.active .bar:nth-child(3) {
            animation-delay: 0.4s;
        }

        @keyframes audio-bounce {

            0%,
            100% {
                height: 4px;
            }

            50% {
                height: 12px;
            }
        }

        /* --- Advanced Sync Effects --- */
        .system-shake {
            animation: shake 0.15s cubic-bezier(.36, .07, .19, .97) both;
        }

        @keyframes shake {

            10%,
            90% {
                transform: translate3d(-1px, 0, 0);
            }

            20%,
            80% {
                transform: translate3d(2px, 0, 0);
            }

            30%,
            50%,
            70% {
                transform: translate3d(-4px, 0, 0);
            }

            40%,
            60% {
                transform: translate3d(4px, 0, 0);
            }
        }

        .clock-flash {
            animation: flash-border 0.2s ease-out;
        }

        @keyframes flash-border {
            0% {
                border-color: var(--cyan-neon);
                box-shadow: 0 0 40px var(--cyan-neon);
            }

            100% {
                border-color: rgba(0, 242, 255, 0.4);
                box-shadow: 0 0 20px rgba(0, 242, 255, 0.2);
            }
        }

        body.vibrating {
            animation: system-vibration 0.1s infinite;
        }

        @keyframes system-vibration {
            0% {
                transform: translate(1px, 1px) rotate(0deg);
            }

            10% {
                transform: translate(-1px, -2px) rotate(-0.5deg);
            }

            20% {
                transform: translate(-3px, 0px) rotate(0.5deg);
            }

            30% {
                transform: translate(3px, 2px) rotate(0deg);
            }

            40% {
                transform: translate(1px, -1px) rotate(0.5deg);
            }
        }
    </style>
</head>

<body>

    <!-- SPLASH SCREEN: INTRO ORBIT -->
    <div id="orbit-splash">
        <div class="splash-content">
            <div class="splash-logo-container">
                <img src="img/orbit_poker_chip_v2.png" alt="Orbit Logo" class="splash-logo">
                <div class="splash-scan-line"></div>
            </div>
            <div class="splash-text-box">
                <div class="splash-title">ORBIT ALPHA</div>
                <div class="splash-subtitle">Iniciando Protocolo de Vigilancia...</div>

                <button id="init-scanner-btn" class="element-3d-lift" onclick="startSystemScanner()">
                    ðŸ”¥ INICIAR SOLICITUD ALPHA
                </button>

                <div class="splash-loader-bar">
                    <div class="splash-progress"></div>
                </div>
            </div>
        </div>
    </div>

    <!-- CORNER WATERMARK -->
    <div class="orbit-watermark">
        <img src="img/orbit_poker_chip_v2.png" alt="Orbit Watermark">
    </div>

    <nav>
        <a href="#" class="logo-box">
            <img src="img/orbit_poker_chip_v2.png" alt="Orbit Logo" class="logo-img">
            <div class="logo-text">ORBIT ALPHA</div>
        </a>
        <div class="nav-links">
            <a href="#vision" class="nav-link">VisiÃ³n IA</a>
            <a href="#toolbox" class="nav-link">Herramientas</a>
            <a href="https://www.twitch.tv/srtorrespoker" target="_blank" class="nav-link"
                style="color: #9146ff; display: flex; align-items: center; gap: 5px;">
                <span
                    style="width: 8px; height: 8px; background: #9146ff; border-radius: 50%; box-shadow: 0 0 5px #9146ff;"></span>
                LIVE OPS
            </a>
            <a href="https://t.me/OrbitAlphaComunidad" target="_blank" class="nav-link"
                style="color: var(--cyan-neon);">ðŸ’¬ Comunidad</a>
            <a href="colaboracion.html" class="btn btn-orbit" style="padding: 12px 30px; font-size: 13px;">Solicitar
                Ingreso a la Alpha Unit</a>
        </div>
    </nav>

    <section class="hero">
        <div class="hero-badge">Alpha Project 1.0 // Deployment Ready</div>
        <h1>ORBIT ALPHA 1.0<br><span>EL CEREBRO TÃCTICO</span></h1>
        <p>Domina las mesas y el streaming con la asistencia de <b>Inteligencia Artificial Multimodelo</b>. MÃ¡s que una
            herramienta, un <b>Copiloto Activo e Inteligente</b> que unifica tu carrera profesional.</p>

        <div class="cta-container">
            <a href="colaboracion.html" class="btn btn-orbit">ðŸš€ INICIAR SOLICITUD ALPHA</a>
            <a href="https://t.me/OrbitAllInPoker" target="_blank" class="btn btn-telegram">ðŸ“¡ CANAL DE COMANDANCIA</a>
        </div>
    </section>

    <section class="video-proof-section"
        style="margin-top: -50px; margin-bottom: 80px; position: relative; z-index: 10;">
        <div class="video-container" style="max-width: 1000px; margin: 0 auto; padding: 0 20px;">
            <div class="hero-badge"
                style="margin: 0 auto 20px auto; display: table; border-color: #9146ff; color: #9146ff; background: rgba(145, 70, 255, 0.1);">
                SOCIAL PROOF // @SRTORRESPOKER
            </div>
            <h1 class="glitch-text"
                style="font-size: clamp(40px, 8vw, 80px); line-height: 1.1; margin-bottom: 20px; text-align: center;">
                ORBIT ALL-IN-ONE <br> <span
                    style="color: transparent; -webkit-text-stroke: 1px #fff; opacity: 0.5;">POKER & STREAM
                    MANAGER</span>
            </h1>

            <!-- EVIDENCIA: STREAM HUD REAL -->
            <div class="evidence-card" style="margin-bottom: 40px; text-align: center;">
                <img src="assets/slides/205001.png" alt="HUD TÃ¡ctico en Vivo" loading="lazy"
                    style="width:100%; border: 1px solid var(--cyan-neon); box-shadow: 0 0 20px rgba(0,242,255,0.2); border-radius: 8px;">
                <div
                    style="font-size: 10px; color: var(--cyan-neon); font-family: 'Share Tech Mono'; margin-top: 5px; text-transform: uppercase;">
                    // HUD TÃ¡ctico: Chronos & Session Delta - Live at Twitch</div>
            </div>

            <h3
                style="text-align: center; font-family: 'Orbitron', sans-serif; color: #fff; margin-bottom: 20px; font-size: 20px;">
                <span class="pulse-led"
                    style="display:inline-block; width:10px; height:10px; background:#ff0055; border-radius:50%; margin-right:10px; box-shadow:0 0 10px #ff0055;"></span>
                <span style="color: #ff0055; text-shadow: 0 0 10px rgba(255, 0, 85, 0.5);">WATCH SR TORRES IN
                    OPERATION</span>
            </h3>

            <!-- CONTENEDOR DE VIDEO/CLIP -->
            <div class="twitch-embed-wrapper"
                style="position: relative; padding-bottom: 56.25%; height: 0; background: #000; border: 1px solid #333; border-radius: 12px; overflow: hidden; box-shadow: 0 0 40px rgba(145, 70, 255, 0.15);">
                <iframe src="https://player.twitch.tv/?channel=srtorrespoker&parent=srtorresredes.github.io&muted=true"
                    height="100%" width="100%" allowfullscreen>
                </iframe>
            </div>

            <div style="text-align: center; margin-top: 20px;">
                <a href="https://www.twitch.tv/srtorrespoker" target="_blank" class="btn"
                    style="background: #9146ff; color: #fff; font-weight: 800; padding: 10px 25px; border-radius: 6px; text-decoration: none; display: inline-flex; align-items: center; gap: 10px; transition: all 0.3s; animation: pulse-btn 2s infinite;">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                        <path
                            d="M11.571 4.714h1.715v5.143H11.57zm4.715 0H18v5.143h-1.714zM6 0L1.714 4.286v15.428h5.143V24l4.286-4.286h3.428L22.286 12V0zm14.571 11.143l-3.428 3.429h-3.429l-3 3v-3H6.857V1.714h13.714Z" />
                    </svg>
                    VER OPERACIÃ“N EN VIVO
                </a>
            </div>
        </div>
    </section>

    <style>
        @keyframes pulse-led {
            0% {
                opacity: 1;
            }

            50% {
                opacity: 0.3;
            }

            100% {
                opacity: 1;
            }
        }

        @keyframes pulse-btn {
            0% {
                box-shadow: 0 0 0 0 rgba(145, 70, 255, 0.7);
            }

            70% {
                box-shadow: 0 0 0 10px rgba(145, 70, 255, 0);
            }

            100% {
                box-shadow: 0 0 0 0 rgba(145, 70, 255, 0);
            }
        }

        .scan-line-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, transparent 50%, rgba(0, 242, 255, 0.02) 51%);
            background-size: 100% 4px;
            pointer-events: none;
            z-index: 5;
        }
    </style>

    <section class="ai-section" id="vision">
        <div class="ai-container">
            <div class="ai-text">
                <div class="hero-badge"
                    style="border-color: var(--magenta-neon); color: var(--magenta-neon); background: rgba(255, 0, 255, 0.1);">
                    Voz Multimodelo v.2026</div>
                <h2
                    style="font-family: 'Orbitron', sans-serif; font-size: clamp(30px, 5vw, 50px); color: #fff; margin-bottom: 30px; line-height: 1.1;">
                    UNA VOZ, TRES GIGANTES.</h2>
                <div class="evidence-card" style="margin-bottom: 25px;">
                    <!-- EVIDENCIA: INTERFAZ NEURAL & COMANDOS -->
                    <img src="assets/slides/205010.png" alt="Interfaz de Voz" loading="lazy"
                        style="width:100%; border-left: 3px solid var(--magenta-neon); border-radius: 4px;">
                    <div
                        style="font-size: 10px; color: var(--magenta-neon); font-family: 'Share Tech Mono'; margin-top: 5px;">
                        // VisualizaciÃ³n Espectral de Comandos de Voz</div>
                </div>
                <p style="font-size: 19px; color: var(--text-dim); margin-bottom: 30px;">Orbit no depende de un solo
                    cerebro. Nuestra arquitectura hÃ­brida alterna entre <b>Groq (Latencia Ultra-Baja)</b>, <b>OpenAI
                        (Razonamiento Complejo)</b> y <b>Gemini (AnÃ¡lisis Global)</b> para que tu voz sea ley en
                    milisegundos.</p>
                <div
                    style="margin-bottom: 25px; padding: 15px; background: rgba(0,255,157,0.05); border-left: 3px solid var(--cyan-neon); border-radius: 4px;">
                    <b
                        style="color: var(--cyan-neon); display: block; margin-bottom: 5px; font-size: 11px; text-transform: uppercase;">//
                        STRATEGY TIP by @SRTORRESPOKER</b>
                    <p style="font-size: 14px; margin: 0; color: #ddd; font-style: italic;">"Utilizo el <b
                            style="color:#fff">Chronos Journal</b> para dictar notas rÃ¡pidas de manos marcadas sin dejar
                        de mirar la acciÃ³n en las otras mesas. Al terminar la sesiÃ³n, mi estudio ya estÃ¡ organizado."
                    </p>
                </div>
                <!-- STREAMER SETUP SECTION -->
                <div style="margin-top: 30px; border: 1px dashed var(--text-dim); padding: 15px; border-radius: 8px;">
                    <h4 style="color: #fff; margin-bottom: 10px;">ðŸŽ¥ STREAMER SETUP (OBS)</h4>
                    <ul style="font-size: 13px; color: var(--text-dim); list-style: none; padding-left: 0;">
                        <li>â€¢ Add Source: <b>Browser Source</b></li>
                        <li>â€¢ URL: <b>.../overlay_obs.html</b></li>
                        <li>â€¢ Filter: <b>Color Key (Custom Color #000000)</b></li>
                        <li>â€¢ Dim: <b>1920x1080 (Fit to Screen)</b></li>
                    </ul>
                </div>
                <ul style="color: var(--cyan-neon); list-style: none; font-weight: 700; font-size: 16px;">
                    <li style="margin-bottom: 15px;">âš¡ Inferencia en Tiempo Real < 50ms</li>
                    <li style="margin-bottom: 15px;">ðŸ”Š Reconocimiento FonÃ©tico Poker-Specific</li>
                    <li style="margin-bottom: 15px;">ðŸ›¡ï¸ Modo Offline de Emergencia</li>
                </ul>
            </div>
            <div class="ai-visual">
                <!-- EVIDENCIA: LOBBY CENTRAL -->
                <div style="position: relative; margin-bottom: 20px;">
                    <img src="assets/slides/205002.png" alt="Dashboard Principal" loading="lazy"
                        style="width:100%; border-radius: 12px; border: 1px solid rgba(255,255,255,0.1); box-shadow: 0 20px 50px rgba(0,0,0,0.5);">
                    <div
                        style="position: absolute; bottom: 10px; right: 10px; background: rgba(0,0,0,0.8); padding: 5px 10px; border-radius: 4px; font-size: 9px; color: #fff; font-family: monospace;">
                        DASHBOARD V2.0 // LIVE</div>
                    <div class="scan-line-overlay"></div>
                </div>

                <!-- EVIDENCIA: BÃ“VEDA MAESTRA -->
                <div style="position: relative;">
                    <img src="assets/slides/205007.png" alt="BÃ³veda Maestra" loading="lazy"
                        style="width:100%; border-radius: 12px; border: 1px solid rgba(0,255,0,0.2); box-shadow: 0 10px 40px rgba(0,255,0,0.05);">
                    <div
                        style="position: absolute; bottom: 10px; right: 10px; background: rgba(0,0,0,0.8); padding: 5px 10px; border-radius: 4px; font-size: 9px; color: #0f9; font-family: monospace;">
                        VAULT CORE // SECURE</div>
                </div>
            </div>
        </div>
    </section>

    .scan-line-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to bottom, transparent 50%, rgba(0, 242, 255, 0.02) 51%);
    background-size: 100% 4px;
    pointer-events: none;
    z-index: 5;
    }
    </style>

    <section class="philosophy-section" id="filosofia">
        <div class="section-header" style="text-align: center; margin-bottom: 100px;">
            <div class="hero-badge">Nuestra FilosofÃ­a // El PorquÃ©</div>
            <h2
                style="font-family: 'Outfit', sans-serif; font-size: clamp(35px, 6vw, 65px); color: #fff; margin-bottom: 25px; line-height: 1;">
                NUESTRA FILOSOFÃA:<br><span style="color: var(--cyan-neon);">POR QUÃ‰ ORBIT</span>
            </h2>
            <p style="font-size: 19px; color: var(--text-dim); max-width: 900px; margin: 0 auto; line-height: 1.6;">
                Ya no se trata solo de jugar cartas; se trata de gestionar un ecosistema complejo de datos, finanzas y
                entretenimiento en tiempo real. Orbit nace para eliminar la <b>fragmentaciÃ³n caÃ³tica</b>.
            </p>
        </div>

        <div class="glitch-grid">
            <!-- Card 1: Caos Operativo -->
            <div class="glitch-card">
                <div class="glitch-title">01. Caos Operativo</div>
                <p>Jugar 12+ mesas requiere una capacidad de procesamiento sobrehumana. Monitorear rangos y HUDs
                    mientras decides en segundos genera una fatiga cognitiva inmensa. Orbit centraliza el control para
                    que solo pienses en el <b>A-Game</b>.</p>
                <div class="glitch-line"></div>
            </div>

            <!-- Card 2: GestiÃ³n Financiera -->
            <div class="glitch-card">
                <div class="glitch-title">02. GestiÃ³n CrÃ­tica</div>
                <p>La varianza es el enemigo silencioso. Sin un control estricto del Bankroll, incluso los ganadores
                    pueden quebrar. Las hojas de cÃ¡lculo son obsoletas; Orbit automatiza la protecciÃ³n de tu capital con
                    precisiÃ³n matemÃ¡tica.</p>
                <div class="glitch-line"></div>
            </div>

            <!-- Card 3: Carga del Streamer -->
            <div class="glitch-card">
                <div class="glitch-title">03. Carga del Streamer</div>
                <p>Los creadores de contenido tienen doble carga: jugar su mejor pÃ³ker y entretener a la audiencia.
                    Orbit unifica la producciÃ³n de streaming bajo una interfaz automatizada, transformando el juego en
                    un espectÃ¡culo visual.</p>
                <div class="glitch-line"></div>
            </div>
        </div>

        <div style="margin-top: 120px; text-align: center;">
            <div class="copilot-banner">
                <span class="copilot-tag">// RESUMEN EJECUTIVO</span>
                <h3
                    style="font-size: clamp(28px, 5vw, 48px); font-weight: 900; color: #fff; letter-spacing: -1px; line-height: 1.1;">
                    MUCHAS HERRAMIENTAS SON PASIVAS.<br>
                    <span style="color: var(--cyan-neon); text-shadow: 0 0 30px var(--cyan-glow);">ORBIT ES UN COPILOTO
                        ACTIVO E INTELIGENTE.</span>
                </h3>
                <p
                    style="margin-top: 30px; color: var(--text-dim); font-size: 17px; max-width: 800px; margin-left: auto; margin-right: auto;">
                    Nacemos para que tomes mejores decisiones (ganes mÃ¡s dinero) y ofrezcas un mejor espectÃ¡culo (ganes
                    mÃ¡s seguidores), reduciendo tu estrÃ©s operativo al mÃ­nimo absoluto.
                </p>
            </div>
        </div>
    </section>

    <section class="toolbox" id="#toolbox">
        <div class="section-header">
            <h2 style="font-family: 'Outfit', sans-serif; font-size: 45px; margin-bottom: 20px;">ARSENAL DE ALTO
                RENDIMIENTO</h2>
            <p style="color: var(--text-dim); font-size: 20px;">MÃ¡s de 80 herramientas integradas en un <b>Copiloto
                    Activo e Inteligente</b>.</p>
        </div>

        <div class="category-group">
            <div class="category-title">ðŸ§  ANÃLISIS DE RANGOS & IA</div>
            <div class="tool-grid">
                <div class="tool-card">
                    <span class="tool-icon">ðŸ§ </span>
                    <h3>Interfaz Neural</h3>
                    <p>No toques el ratÃ³n. MantÃ©n el foco en las cartas. La IA te susurra tu balance.</p>
                    <span class="tag">Voice Engine</span>
                </div>
                <div class="tool-card">
                    <span class="tool-icon">ðŸ“š</span>
                    <h3>Logic Analyzer</h3>
                    <p>Herramienta que audita tu toma de decisiones y analiza las desviaciones en tu lÃ­nea de
                        pensamiento.</p>
                    <span class="tag">Strategic Audit</span>
                </div>
                <div class="tool-card">
                    <span class="tool-icon">ðŸ“œ</span>
                    <h3>Vocab Poker-Sync</h3>
                    <p>Diccionario fonÃ©tico adaptado a la jerga del pÃ³ker (Stakes, Reraises, Flop textures) para mayor
                        precisiÃ³n.</p>
                    <span class="tag">Voice Engine</span>
                </div>
            </div>
        </div>

        <div class="category-group">
            <div class="category-title">ðŸ“Š GESTIÃ“N DE BANCA Y FINANZAS</div>
            <div class="tool-grid">
                <div class="tool-card">
                    <span class="tool-icon">ðŸ›ï¸</span>
                    <h3>BÃ³veda Maestra</h3>
                    <p>Tu Ãºnica fuente de verdad. El Banco Central donde se teletransportan tus ganancias.</p>
                    <span class="tag">Security Core</span>
                </div>
                <div class="tool-card">
                    <span class="tool-icon">âš–ï¸</span>
                    <h3>Session Delta</h3>
                    <p>MÃ©trica viva que calcula tu rendimiento en tiempo real frente a la varianza esperada.</p>
                    <span class="tag">Live Metric</span>
                </div>
                <div class="tool-card">
                    <span class="tool-icon">ðŸŒ</span>
                    <h3>Multi-Room Sync</h3>
                    <p>SincronizaciÃ³n nativa con Winamax, PokerStars, GG, 888 y las principales salas Cripto globales.
                    </p>
                    <span class="tag">Native Bridge</span>
                </div>
            </div>
        </div>

        <div class="category-group">
            <div class="category-title">ðŸ‘¾ AUTOMATIZACIÃ“N DE STREAM</div>
            <div class="tool-grid">
                <div class="tool-card">
                    <span class="tool-icon">ðŸŽ­</span>
                    <h3>HUD TÃ¡ctico</h3>
                    <p>Cada pÃ­xel estÃ¡ diseÃ±ado para el stream. Limpio, potente y futurista.</p>
                    <span class="tag">Graphics v2.0</span>
                </div>
                <div class="tool-card">
                    <span class="tool-icon">ðŸš¦</span>
                    <h3>SemÃ¡foro de Voz</h3>
                    <p>Indicador visual de estado de IA que permite a tus viewers saber cuÃ¡ndo estÃ¡s operando manos
                        libres.</p>
                    <span class="tag">Visual Comms</span>
                </div>
                <div class="tool-card">
                    <span class="tool-icon">ðŸ•°ï¸</span>
                    <h3>Chronos Journal</h3>
                    <p>Diario de sesiones automatizado que registra notas por voz durante el fragor de la batalla.</p>
                    <span class="tag">Journaling IA</span>
                </div>
            </div>
        </div>
    </section>

    <section class="faq-section" id="faq" style="margin-top: 100px; margin-bottom: 50px;">
        <div class="faq-container" style="max-width: 900px; margin: 0 auto; padding: 0 20px;">
            <div class="section-header" style="text-align: center; margin-bottom: 50px;">
                <h2 style="font-family: 'Outfit', sans-serif; font-size: 36px; color: #fff; text-transform: uppercase;">
                    Preguntas Frecuentes <span style="color: var(--cyan-neon);">// FAQ</span>
                </h2>
            </div>

            <div class="faq-grid" style="display: grid; gap: 20px;">
                <!-- Q1 -->
                <div class="faq-item"
                    style="background: rgba(0, 20, 10, 0.4); border: 1px solid rgba(0, 255, 157, 0.1); border-radius: 12px; padding: 25px;">
                    <h3 style="color: var(--cyan-neon); font-size: 18px; margin-bottom: 10px; font-weight: 700;">Â¿Orbit
                        juega por mÃ­? (RTA/Bot)</h3>
                    <p style="color: var(--text-dim); line-height: 1.6;"><b>ABSOLUTAMENTE NO.</b> Orbit es un sistema de
                        gestiÃ³n pasiva y anÃ¡lisis post-sesiÃ³n. Durante el juego, actÃºa como un "Caddy Digital" (gestiÃ³n
                        de bankroll, control de horarios, notas rÃ¡pidas). No ofrece asistencia en tiempo real (RTA) y
                        cumple estrictamente con los T&C de las salas como PokerStars y Winamax.</p>
                </div>

                <!-- Q2 -->
                <div class="faq-item"
                    style="background: rgba(0, 20, 10, 0.4); border: 1px solid rgba(0, 255, 157, 0.1); border-radius: 12px; padding: 25px;">
                    <h3 style="color: var(--cyan-neon); font-size: 18px; margin-bottom: 10px; font-weight: 700;">Â¿Es
                        compatible con Mac/M1?</h3>
                    <p style="color: var(--text-dim); line-height: 1.6;">La Unidad Alpha 1.0 es una Web App Progresiva
                        (PWA) optimizada para navegadores Chromium (Chrome/Edge/Brave). Funciona en Mac, pero la
                        integraciÃ³n de comandos de voz nativos estÃ¡ optimizada actualmente para Windows 10/11.</p>
                </div>

                <!-- Q3 -->
                <div class="faq-item"
                    style="background: rgba(0, 20, 10, 0.4); border: 1px solid rgba(0, 255, 157, 0.1); border-radius: 12px; padding: 25px;">
                    <h3 style="color: var(--cyan-neon); font-size: 18px; margin-bottom: 10px; font-weight: 700;">Â¿CÃ³mo
                        funciona la BÃ³veda de Bankroll?</h3>
                    <p style="color: var(--text-dim); line-height: 1.6;">Orbit no se conecta a tu banco real. TÃº
                        introduces tus saldos iniciales y el sistema simula un "Banco Central" donde tus ganancias de
                        diferentes salas se unifican. Es una herramienta de contabilidad visual de alta precisiÃ³n.</p>
                </div>

                <!-- Q4 / TESTIMONY -->
                <div class="faq-item"
                    style="background: rgba(0, 20, 10, 0.4); border: 1px solid var(--magenta-neon); border-radius: 12px; padding: 25px; box-shadow: 0 0 20px rgba(255, 0, 255, 0.1);">
                    <h3 style="color: var(--magenta-neon); font-size: 18px; margin-bottom: 10px; font-weight: 700;">//
                        TESTIMONIO DEL CREADOR</h3>
                    <p style="color: #fff; line-height: 1.6; font-style: italic;">"Esto no es software de terceros. Es
                        una extensiÃ³n de mi mente diseÃ±ada para el stream. Probado cada dÃ­a en el frente de batalla en
                        <a href='https://twitch.tv/srtorrespoker' target='_blank'
                            style='color:var(--magenta-neon); text-decoration:none;'>twitch.tv/srtorrespoker</a>."
                    </p>
                    <div style="margin-top: 10px; text-align: right; font-size: 11px; color: var(--magenta-neon);">â€”
                        @SrTorresPoker, Fundador Orbit Unit</div>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <div class="footer-grid">
            <div class="footer-col">
                <h4>PROYECTO ORBIT</h4>
                <p style="font-size: 13px; color: var(--text-dim);">La evoluciÃ³n del jugador de pÃ³ker hacia el
                    arquitecto tecnolÃ³gico.</p>
                <div style="margin-top: 30px; display: flex; gap: 20px;">
                    <a href="https://t.me/OrbitAllInPoker" style="font-size: 22px;">ðŸ“¢</a>
                    <a href="https://t.me/OrbitAlphaComunidad" style="font-size: 22px;">ðŸ’¬</a>
                </div>
            </div>
            <div class="footer-col">
                <h4>NAVEGACIÃ“N</h4>
                <a href="#vision">MisiÃ³n IA</a>
                <a href="#toolbox">Ecosistema</a>
                <a href="colaboracion.html">Registro Alpha</a>
            </div>
            <div class="footer-col">
                <h4>LEGAL / SEGURIDAD</h4>
                <a href="#">Privacidad Local</a>
                <a href="#">Security Protocol</a>
                <a href="#">Â© 2026 Orbit Systems</a>
            </div>
        </div>
        <p class="copyright">SISTEMA DESPLEGADO POR @SRTORRESPOKER | UNIDAD ALPHA 1.0</p>
    </footer>

    <div class="live-status">
        <div class="pulse-dot"></div>
        <span
            style="font-size: 12px; font-weight: 900; letter-spacing: 2px; color: var(--cyan-neon); margin-right: 15px;">PROTOCOL
            ONLINE</span>

        <!-- AUDIO MISSION CONTROL -->
        <div id="mission-audio-control" class="audio-toggle" onclick="toggleMissionMusic()"
            title="Sincronizar Banda Sonora">
            <div class="audio-bars">
                <div class="bar"></div>
                <div class="bar"></div>
                <div class="bar"></div>
            </div>
            <span id="audio-status-text"
                style="font-size: 10px; font-weight: 700; color: var(--text-dim); letter-spacing: 1px;">SOUND:
                OFF</span>
        </div>
    </div>

    <!-- SOUNDTRACK ELEMENT (Async) -->
    <audio id="official-soundtrack" loop preload="none">
        <source src="assets/sounds/mission_factory.wav" type="audio/wav">
    </audio>
    <audio id="heartbeat-audio" loop preload="none">
        <source src="assets/sounds/heartbeat.mp3" type="audio/mpeg">
    </audio>
    <audio id="clock-audio" loop preload="none">
        <source src="assets/sounds/clock.mp3" type="audio/mpeg">
    </audio>

    <style>
        @keyframes wave {

            0%,
            100% {
                transform: scaleY(1);
            }

            50% {
                transform: scaleY(1.5);
            }
        }
    </style>

    <script>
        // Splash Screen Logic (Manual Trigger)
        // window.addEventListener('load', () => { /* Auto-fade removed for manual start */ });

        function startSystemScanner() {
            const btn = document.getElementById('init-scanner-btn');
            const progress = document.querySelector('.splash-progress');
            const splash = document.getElementById('orbit-splash');

            // 1. Activate UI
            if (btn) btn.style.display = 'none'; // Hide button
            if (progress) {
                progress.style.width = '0%'; // Reset
                // Force reflow
                void progress.offsetWidth;
                progress.classList.add('scanning');
            }

            // 2. Start Audio System (User Gesture) - PHASE 1: TENSION
            initAudioSync();
            // Ensure context is running
            if (audioCtx.state === 'suspended') audioCtx.resume();

            // Play Tension Layer
            clock.play();
            heartbeat.play();

            // Update UI State
            audioControl.classList.add('active');
            audioText.innerText = "SOUND: ON";
            audioText.style.color = "var(--cyan-neon)";

            // 3. Wait for Scan (4s) then Fade Out & Start Mission
            setTimeout(() => {
                if (splash) {
                    splash.classList.add('fade-out');
                    setTimeout(() => splash.remove(), 1500);

                    // PHASE 2: MISSION (Main Track)
                    audio.play();
                    // Optional: Fade out clock/heartbeat? For now, let's keep them as layers or stop them.
                    // The user wants "Clock (Start), Heartbeat (Scan), Mission (Final)".
                    // Usually Mission covers everything. Let's stop the others to be clean.
                    clock.pause();
                    heartbeat.pause();
                }
            }, 4000); // 4s scan exact
        }

        // Official Soundtrack Engine (Sync V2)
        const audio = document.getElementById('official-soundtrack');
        const heartbeat = document.getElementById('heartbeat-audio');
        const clock = document.getElementById('clock-audio');
        const audioControl = document.getElementById('mission-audio-control');
        const audioText = document.getElementById('audio-status-text');

        // Visual Target Elements
        const splashLogo = document.querySelector('.splash-logo');
        const headerLogo = document.querySelector('.logo-img');
        const waterMark = document.querySelector('.orbit-watermark img');

        let audioCtx, analyser, dataArray, source;
        let isSyncing = false;

        function initAudioSync() {
            if (audioCtx) return;

            audioCtx = new (window.AudioContext || window.webkitAudioContext)();
            analyser = audioCtx.createAnalyser();
            analyser.fftSize = 256;

            const bufferLength = analyser.frequencyBinCount;
            dataArray = new Uint8Array(bufferLength);

            // Connect main audio
            source = audioCtx.createMediaElementSource(audio);
            source.connect(analyser);
            analyser.connect(audioCtx.destination);

            // Connect heartbeat to destination (direct)
            // Note: We want to visualize heartbeat too if it's playing alone? 
            // For now, let's just route them to speakers. 
            // If we want the logo to pulse to heartbeat, we need to connect logic.
            // But main visualization is hooked to 'audio' (Mission).
            // Let's hook heartbeat to analyzer too for the intro effect!
            const hSource = audioCtx.createMediaElementSource(heartbeat);
            const hGain = audioCtx.createGain();
            hSource.connect(hGain);
            hGain.connect(analyser); // Heartbeat goes to analyzer
            hGain.connect(audioCtx.destination); // And to speakers

            const cSource = audioCtx.createMediaElementSource(clock);
            cSource.connect(audioCtx.destination); // Clock just sounds

            isSyncing = true;
            renderSync();
        }

        function renderSync() {
            if (!isSyncing) return;
            requestAnimationFrame(renderSync);

            analyser.getByteFrequencyData(dataArray);

            // 1. Analyze Core Intensity (Bass range) for Vibration
            let sum = 0;
            for (let i = 0; i < 5; i++) sum += dataArray[i];
            const bassIntensity = sum / 5;

            // 2. Trigger System Shake on high intensity peaks
            if (bassIntensity > 200) {
                document.body.classList.add('vibrating');
            } else {
                document.body.classList.remove('vibrating');
            }

            // 3. Dynamic Glow based on average intensity
            const avgIntensity = dataArray.reduce((a, b) => a + b) / dataArray.length;
            const glowSize = 10 + (avgIntensity / 2);
            const opacity = 0.3 + (avgIntensity / 255);

            const glowStyle = `0 0 ${glowSize}px rgba(0, 242, 255, ${opacity})`;
            if (splashLogo) splashLogo.style.boxShadow = glowStyle;
            if (headerLogo) headerLogo.style.boxShadow = glowStyle;
        }

        function toggleMissionMusic() {
            initAudioSync();

            if (audio.paused && clock.paused && heartbeat.paused) {
                // Manual toggle (Reviewer mode): Play everything
                if (audioCtx.state === 'suspended') audioCtx.resume();

                audio.play();
                // heartbeat.play(); // Optional in manual mode
                // clock.play();

                audioControl.classList.add('active');
                audioText.innerText = "SOUND: ON";
                audioText.style.color = "var(--cyan-neon)";
            } else {
                audio.pause();
                heartbeat.pause();
                clock.pause();

                audioControl.classList.remove('active');
                audioText.innerText = "SOUND: OFF";
                audioText.style.color = "var(--text-dim)";
                document.body.classList.remove('vibrating');
            }
        }
        // Clock Sync Logic: Accurate flash every 1s
        clock.addEventListener('play', () => {
            const flashInterval = setInterval(() => {
                if (clock.paused) {
                    clearInterval(flashInterval);
                    return;
                }
                const logo = document.querySelector('.logo-img');
                if (logo) {
                    logo.classList.add('clock-flash');
                    setTimeout(() => logo.classList.remove('clock-flash'), 200);
                }
            }, 1000);
        });

        // Auto-play attempt on first interaction
        document.body.addEventListener('click', () => {
            // Optional: init on first click to avoid audio issues
            // initAudioSync();
        }, { once: true });
    </script>
</body>

</html>
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orbit Alpha 1.0 // Centro de Reclutamiento</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=Share+Tech+Mono&display=swap"
        rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="orbit_icon.ico">

    <!-- ORBIT CONFIG: API Keys Loader -->
    <script src="config.js"></script>

    <style>
        :root {
            --bg-dark: #02050a;
            --card-bg: rgba(10, 15, 25, 0.95);
            --cyan-neon: #00f2ff;
            --cyan-glow: rgba(0, 242, 255, 0.3);
            --magenta-neon: #ff00ff;
            --magenta-glow: rgba(255, 0, 255, 0.2);
            --text-main: #f8fafc;
            --text-dim: #94a3b8;
            --accent: #00ffd0;
            --font-display: 'Orbitron', sans-serif;
            --font-tech: 'Share Tech Mono', monospace;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-tech);
            background-color: var(--bg-dark);
            background-image:
                radial-gradient(circle at 10% 20%, rgba(0, 242, 255, 0.1) 0%, transparent 45%),
                radial-gradient(circle at 90% 80%, rgba(255, 0, 255, 0.08) 0%, transparent 45%),
                url('https://www.transparenttextures.com/patterns/carbon-fibre.png');
            color: var(--text-main);
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 60px 20px;
        }

        .form-container {
            background: var(--card-bg);
            backdrop-filter: blur(30px);
            border: 1px solid rgba(0, 242, 255, 0.25);
            border-radius: 35px;
            padding: 60px;
            width: 100%;
            max-width: 850px;
            box-shadow: 0 0 80px rgba(0, 242, 255, 0.15);
            animation: slideUp 1s cubic-bezier(0.2, 1, 0.3, 1);
            position: relative;
            overflow: hidden;
        }

        .form-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(to right, var(--cyan-neon), var(--magenta-neon));
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .welcome-box {
            background: rgba(0, 242, 255, 0.05);
            border-left: 6px solid var(--cyan-neon);
            padding: 30px;
            border-radius: 20px;
            margin-bottom: 50px;
        }

        .welcome-box h2 {
            font-family: var(--font-display);
            font-size: 22px;
            color: var(--cyan-neon);
            margin-bottom: 12px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .welcome-box p {
            font-size: 16px;
            line-height: 1.7;
            color: var(--text-dim);
        }

        .header {
            text-align: center;
            margin-bottom: 50px;
        }

        .orbit-logo {
            font-size: 12px;
            letter-spacing: 8px;
            color: var(--magenta-neon);
            text-transform: uppercase;
            margin-bottom: 20px;
            display: block;
            font-weight: 900;
        }

        h1 {
            font-family: 'Outfit', sans-serif;
            font-size: 38px;
            font-weight: 950;
            color: #fff;
            letter-spacing: -2px;
            text-transform: uppercase;
        }

        .contact-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 25px;
            margin-bottom: 50px;
            padding-bottom: 40px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .input-group {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .input-group label {
            font-size: 12px;
            text-transform: uppercase;
            color: var(--cyan-neon);
            font-weight: 900;
            letter-spacing: 1.5px;
        }

        .input-group input,
        .input-group select {
            background: rgba(0, 0, 0, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            padding: 18px;
            color: #fff;
            font-size: 15px;
            transition: 0.4s;
        }

        .input-group input:focus,
        .input-group select:focus {
            outline: none;
            border-color: var(--cyan-neon);
            box-shadow: 0 0 20px var(--cyan-glow);
            background: #000;
        }

        .full-width {
            grid-column: span 2;
        }

        .section-title {
            font-size: 16px;
            margin-bottom: 25px;
            color: #fff;
            text-transform: uppercase;
            letter-spacing: 3px;
            font-weight: 900;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .section-title::after {
            content: '';
            height: 1px;
            flex: 1;
            background: rgba(255, 255, 255, 0.1);
        }

        .area-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 50px;
        }

        .area-option {
            background: rgba(255, 255, 255, 0.02);
            border: 2px solid rgba(255, 255, 255, 0.05);
            border-radius: 20px;
            padding: 25px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 20px;
            transition: 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .area-option:hover {
            border-color: var(--magenta-neon);
            background: rgba(255, 0, 255, 0.05);
            transform: scale(1.02);
        }

        .area-option.selected {
            background: rgba(255, 0, 255, 0.12);
            border-color: var(--magenta-neon);
            box-shadow: 0 0 30px rgba(255, 0, 255, 0.2);
        }

        .area-icon {
            font-size: 26px;
            width: 55px;
            height: 55px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .area-text b {
            display: block;
            font-size: 17px;
            color: #fff;
            margin-bottom: 4px;
        }

        .area-text span {
            font-size: 13px;
            color: var(--text-dim);
        }

        input[type="checkbox"] {
            display: none;
        }

        .submit-btn {
            width: 100%;
            background: linear-gradient(135deg, var(--cyan-neon), var(--accent));
            color: var(--bg-dark);
            border: none;
            border-radius: 15px;
            padding: 22px;
            font-weight: 950;
            text-transform: uppercase;
            cursor: pointer;
            box-shadow: 0 15px 40px rgba(0, 242, 255, 0.3);
            transition: 0.5s;
            letter-spacing: 3px;
            font-size: 16px;
        }

        .submit-btn:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 60px var(--cyan-glow);
            filter: brightness(1.2);
        }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 35px;
            color: var(--text-dim);
            text-decoration: none;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            font-weight: 700;
            transition: 0.3s;
        }

        .back-link:hover {
            color: var(--cyan-neon);
            letter-spacing: 2px;
        }

        .telegram-links-box {
            margin-top: 50px;
            padding-top: 40px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            display: flex;
            justify-content: center;
            gap: 30px;
        }

        .tg-link {
            display: flex;
            align-items: center;
            gap: 12px;
            color: var(--text-dim);
            text-decoration: none;
            font-size: 14px;
            font-weight: 600;
            transition: 0.3s;
        }

        .tg-link:hover {
            color: var(--cyan-neon);
        }

        .tg-badge {
            background: var(--cyan-neon);
            color: #000;
            padding: 2px 8px;
            border-radius: 4px;
            font-size: 10px;
            font-weight: 900;
        }

        @keyframes spin {
            from {
                transform: rotate(0deg);
            }

            to {
                transform: rotate(360deg);
            }
        }

        @keyframes glitch-text {
            0% {
                transform: translate(0);
                text-shadow: -2px 0 var(--magenta-neon), 2px 0 var(--cyan-neon);
            }

            20% {
                transform: translate(-2px, 2px);
            }

            40% {
                transform: translate(-2px, -2px);
                text-shadow: 2px 0 var(--magenta-neon), -2px 0 var(--cyan-neon);
            }

            60% {
                transform: translate(2px, 2px);
            }

            80% {
                transform: translate(2px, -2px);
                text-shadow: -2px 0 var(--magenta-neon), 2px 0 var(--cyan-neon);
            }

            100% {
                transform: translate(0);
            }
        }

        @media (max-width: 768px) {

            .contact-section,
            .area-grid {
                grid-template-columns: 1fr;
            }

            .full-width {
                grid-column: span 1;
            }

            .form-container {
                padding: 40px 25px;
            }
        }
    </style>
</head>

<body>
    <div class="form-container">
        <div class="welcome-box">
            <h2>ðŸ›°ï¸ CENTRO DE RECLUTAMIENTO ALPHA 1.0</h2>
            <p>Buscamos perfiles de alto nivel: Pro Players con disciplina y Streamers con visiÃ³n tecnolÃ³gica. Tu
                reporte de fallas y sugerencias estratÃ©gicas forjarÃ¡n el futuro de Orbit.</p>
        </div>

        <div class="header">
            <span class="orbit-logo">Connection Status: Encrypted</span>
            <h1>CONEXIÃ“N CON EL NÃšCLEO</h1>
        </div>

        <form id="collab-form">
            <div class="contact-section">
                <div class="input-group full-width">
                    <label>Identidad Digital (Nick o Nombre Real)</label>
                    <input type="text" id="user-name" placeholder="Tu identificador en las mesas" required>
                </div>
                <div class="input-group">
                    <label>E-Mail de ComunicaciÃ³n</label>
                    <input type="email" id="user-email" placeholder="enlace@central.com" required>
                </div>
                <div class="input-group">
                    <label>Telegram (Opcional pero recomendado)</label>
                    <input type="text" id="user-telegram" placeholder="@usuario">
                </div>

                <div class="input-group">
                    <label>Nivel de Juego (Stakes Principales)</label>
                    <select id="user-level" required>
                        <option value="" disabled selected>Nivel tÃ¡ctico</option>
                        <option value="Micro">Micro Stakes (NL2 - NL10)</option>
                        <option value="Low">Low Stakes (NL25 - NL50)</option>
                        <option value="Mid">Mid Stakes (NL100 - NL400)</option>
                        <option value="High">High Stakes (NL500+ / Nosebleeds)</option>
                        <option value="MTT_Low">MTT Low ABI (< 20â‚¬)</option>
                        <option value="MTT_Mid">MTT Mid ABI (20â‚¬ - 100â‚¬)</option>
                        <option value="MTT_High">MTT High ABI (> 100â‚¬)</option>
                    </select>
                </div>

                <div class="input-group">
                    <label>Volumen Mensual y Software</label>
                    <input type="text" id="user-volume" placeholder="Manos/MTTs al mes y software (HM3, H2N...)"
                        required>
                </div>

                <div class="input-group">
                    <label>Enlace Stream / Redes</label>
                    <input type="text" id="user-stream-link" placeholder="Twitch / YouTube / Social" required>
                </div>

                <div class="input-group">
                    <label>Media de Espectadores (si aplica)</label>
                    <input type="text" id="user-viewers" placeholder="CCV Media">
                </div>

                <div class="input-group">
                    <label>ConfiguraciÃ³n de APIs (Modo Zen)</label>
                    <select id="user-api-exp" required>
                        <option value="" disabled selected>Â¿Sabes configurar API Keys?</option>
                        <option value="Experto">SÃ­, tengo experiencia con APIs</option>
                        <option value="Intermedio">He configurado algunas cosas bÃ¡sicas</option>
                        <option value="Principiante">No, pero estoy dispuesto a aprender</option>
                        <option value="Pendiente">Prefiero ayuda tÃ©cnica</option>
                    </select>
                </div>

                <div class="input-group full-width">
                    <label>Compromiso de Reporte de Bugs</label>
                    <select id="user-commit" required>
                        <option value="" disabled selected>Â¿Te comprometes a documentar errores?</option>
                        <option value="Total">SÃ­, documentarÃ© y compartirÃ© fallos en el hilo</option>
                        <option value="Solo_Feedback">Solo feedback ocasional</option>
                        <option value="No">No puedo comprometerme</option>
                    </select>
                </div>
            </div>

            <h3 class="section-title">ROL DE COLABORACIÃ“N</h3>
            <div class="area-grid">
                <label class="area-option" onclick="toggleOption(this)">
                    <input type="checkbox" name="area">
                    <div class="area-icon">ðŸ“Š</div>
                    <div class="area-text"><b>Analista Alpha</b><span>Pruebas de lÃ³gica y banca</span></div>
                </label>
                <label class="area-option" onclick="toggleOption(this)">
                    <input type="checkbox" name="area">
                    <div class="area-icon">ðŸŽ™ï¸</div>
                    <div class="area-text"><b>Vocal Commander</b><span>Testeo de comandos de voz</span></div>
                </label>
                <label class="area-option" onclick="toggleOption(this)">
                    <input type="checkbox" name="area">
                    <div class="area-icon">ðŸ“¡</div>
                    <div class="area-text"><b>Orbit Ambassador</b><span>Streamers y Creadores</span></div>
                </label>
                <label class="area-option" onclick="toggleOption(this)">
                    <input type="checkbox" name="area">
                    <div class="area-icon">ðŸ›¡ï¸</div>
                    <div class="area-text"><b>Inversor / Partner</b><span>ExpansiÃ³n del ecosistema</span></div>
                </label>
            </div>

            <div
                style="margin-top: 20px; text-align: center; font-size: 11px; color: var(--text-dim); margin-bottom: 25px;">
                âš ï¸ <b>AVISO LEGAL:</b> El envÃ­o de esta solicitud no garantiza el acceso. La administraciÃ³n de ORBIT
                revisarÃ¡ tu perfil tÃ©cnico antes de enviarte el enlace de acceso al grupo de desarrollo.
            </div>

            <button type="button" class="submit-btn" onclick="submitForm()">ESTABLECER ENLACE ALPHA <span
                    style="display:block; font-size: 9px; opacity: 0.7; margin-top: 5px;">Lee el manual anclado en
                    Telegram al entrar</span></button>
        </form>

        <a href="index.html" class="back-link">â† Volver al Inicio</a>

        <div class="telegram-links-box">
            <a href="https://t.me/OrbitAllInPoker" target="_blank" class="tg-link">
                <span class="tg-badge">NEWS</span> @OrbitAllInPoker
            </a>
            <a href="https://t.me/OrbitAlphaComunidad" target="_blank" class="tg-link">
                <span class="tg-badge" style="background: var(--magenta-neon);">COMMUNITY</span> @OrbitAlphaComunidad
            </a>
        </div>

    </div>

    <!-- Firebase SDKs -->
    <script src="https://www.gstatic.com/firebasejs/8.10.0/firebase-app.js"></script>
    <script src="https://www.gstatic.com/firebasejs/8.10.0/firebase-firestore.js"></script>

    <!-- Alpha Registration Module -->
    <script src="js/services/alpha-registration.js"></script>

    <script>
        function toggleOption(el) {
            const cb = el.querySelector('input');
            setTimeout(() => {
                if (cb.checked) el.classList.add('selected');
                else el.classList.remove('selected');
            }, 10);
        }

        async function submitForm() {
            const name = document.getElementById('user-name').value;
            const email = document.getElementById('user-email').value;
            const telegram = document.getElementById('user-telegram').value;
            const level = document.getElementById('user-level').value;
            const volume = document.getElementById('user-volume').value;
            const streamLink = document.getElementById('user-stream-link').value;
            const viewers = document.getElementById('user-viewers').value;
            const apiExp = document.getElementById('user-api-exp').value;
            const commit = document.getElementById('user-commit').value;
            const btn = document.querySelector('.submit-btn');

            if (!name || !email || !level || !volume || !streamLink) {
                alert('âš ï¸ PROTOCOLO INCOMPLETO: Todos los campos marcados como obligatorios deben ser procesados.');
                return;
            }

            const selectedAreas = Array.from(document.querySelectorAll('input[name="area"]:checked'))
                .map(cb => cb.closest('.area-option').querySelector('.area-text b').textContent);

            btn.textContent = "ðŸ“¡ SINCRONIZANDO DATOS CON EL CORE DE ORBIT...";
            btn.disabled = true;
            btn.style.opacity = '0.6';

            try {
                await handleRegistroAlpha(email, {
                    nombre: name,
                    telegram: telegram || 'Pendiente',
                    nivel_poker: level,
                    volumen_software: volume,
                    enlace_stream: streamLink,
                    media_viewers: viewers || 'N/A',
                    experiencia_api: apiExp,
                    compromiso_bugs: commit,
                    roles: selectedAreas.join(', ') || 'Explorador General'
                });

                btn.textContent = "âœ… SOLICITUD ENVIADA AL BÃšNKER";
                btn.style.background = 'linear-gradient(135deg, #00ff00, #004400)';

                setTimeout(() => {
                    document.getElementById('collab-form').innerHTML = `
                        <div style="text-align: center; padding: 40px; animation: glitch-text 0.5s;">
                            <img src="img/orbit_poker_chip_v2.png" style="width: 120px; animation: spin 4s infinite linear; border-radius: 50%; border: 2px solid var(--cyan-neon); box-shadow: 0 0 30px var(--cyan-glow);">
                            <h2 style="font-family: 'Outfit', sans-serif; color: var(--cyan-neon); margin-top: 30px;">PROCESAMIENTO COMPLETADO</h2>
                            <p style="color: var(--text-dim); margin-top: 15px;">Tus datos han sido segmentados y encriptados.<br>Espera nuestra respuesta en Telegram.</p>
                            <a href="https://t.me/OrbitAlphaComunidad" class="btn btn-orbit" style="display:inline-block; margin-top: 30px; text-decoration: none; background: var(--cyan-neon); color: #000; padding: 15px 30px; border-radius: 10px; font-weight: 900;">ACCEDER AL CANAL ALPHA</a>
                        </div>
                    `;
                }, 1500);
            } catch (error) {
                console.error('Error:', error);
                btn.textContent = "âŒ ERROR DE CONEXIÃ“N";
                btn.style.background = 'linear-gradient(135deg, #ff0000, #440000)';

                setTimeout(() => {
                    btn.textContent = "ESTABLECER ENLACE ALPHA";
                    btn.disabled = false;
                    btn.style.opacity = '1';
                    btn.style.background = '';
                }, 3000);
            }
        }
    </script>
</body>

</html>
@charset "UTF-8";

/* ==========================================
   1. TEMA POKERSTARS (Neon Green & Navy)
   ========================================== */
/* 
    ========================================================================
     🛰️ [MASTER AGENT PROTOCOL] - ORBIT ALL-IN-ONE 2.026 - LECTURA OBLIGATORIA
    ========================================================================
    REGLA: Antes de editar este código, lee 'PROTOCOLO_AGENTES_ORBIT.md'.
    REGLA: Auditar cambios y emitir 'window.OrbitSound.playSuccess()' al finalizar.
    ========================================================================
*/

:root {
  /* ==========================================
     0. SEMANTIC DESIGN TOKENS (BASE LAYER)
     ========================================== */
  --color-accent-primary: #00ff9d;
  /* Main Brand Color */
  --color-accent-secondary: #00f2fe;
  /* Secondary Brand Color */
  --color-status-success: #00ff9d;
  --color-status-danger: #ff4d4d;

  /* Fallbacks for legacy tokens */
  --neon-color: var(--color-accent-primary);
  --cyan-neon: var(--color-accent-secondary);
  --positive-color: var(--color-status-success);
  --negative-color: var(--color-status-danger);

  /* ==========================================
     Z-INDEX LAYER SYSTEM (SEMANTIC)
     ========================================== */
  --z-base: 1;
  /* Default content */
  --z-ui: 100;
  /* Standard UI elements (Taskbar, Sidebar) */
  --z-overlay: 500;
  /* Dropdowns, Tooltips */
  --z-modal: 1000;
  /* Modals, Dialogs */
  --z-notification: 2000;
  /* Toasts, Alerts */
  --z-debug: 9999;
  /* Debug overlays, Critical Catch-alls */
}

/* ==========================================
   1. TEMA POKERSTARS (Default)
   ========================================== */
:root,
[data-theme="poker"] {
  --bg-color: #050a14;
  --card-bg: #0b162a;

  /* Theme Map */
  --color-accent-primary: #00ff9d;
  --color-accent-secondary: #00ff9d;
  /* Mono-accent for Poker theme */

  /* Legacy Mappings (Auto-updated via Semantic Layer) */
  --neon-color: var(--color-accent-primary);
  --cyan-neon: var(--color-accent-secondary);

  --text-main: #ffffff;
  --text-secondary: #8899a6;
  --border-color: rgba(0, 255, 157, 0.4);
  --input-bg: #030810;
  --input-border: #00ff9d;
  --glow-blue: 0 0 12px rgba(0, 255, 157, 0.25);
  --positive-color: #00ff9d;
  --negative-color: #ff4d4d;
  --marfil-solido: #f5f5f5;
  --shadow: 0 8px 32px rgba(0, 0, 0, 0.5);
  --font-main: 'Inter', system-ui, -apple-system, sans-serif;
  --font-mono: 'Poppins', monospace;
  --glass-blur: blur(0px);

  /* PREMIUM 3D DESIGN SYSTEM TOKENS (V2) */
  --glass-3d-bg: rgba(255, 255, 255, 0.05);
  --glass-3d-border: rgba(255, 255, 255, 0.15);
  --glass-3d-shadow: 0 15px 45px rgba(0, 0, 0, 0.6), inset 0 0 15px rgba(255, 255, 255, 0.05);
  --neon-glow-high: 0 0 25px var(--neon-color), 0 0 50px rgba(0, 255, 157, 0.25);
  --depth-shadow-3d: 0 30px 60px rgba(0, 0, 0, 0.7);
  --bevel-light: inset 1px 1px 2px rgba(255, 255, 255, 0.15), inset -1px -1px 2px rgba(0, 0, 0, 0.5);
  --transition-premium: all 0.5s cubic-bezier(0.19, 1, 0.22, 1);
  --transition-bounce: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

/* ==========================================
   2. TEMA AZURE (Blue & Soft Navy)
   ========================================== */
[data-theme="blue"] {
  --bg-color: #1a1f2e;
  --card-bg: rgba(36, 43, 61, 0.9);

  --color-accent-primary: #8ab4f8;
  --color-accent-secondary: #8ab4f8;
  --neon-color: var(--color-accent-primary);
  --cyan-neon: var(--color-accent-secondary);

  --text-main: #e8eaed;
  --text-secondary: #9aa0a6;
  --border-color: rgba(138, 180, 248, 0.3);
  --input-bg: #2a3142;
  --input-border: rgba(138, 180, 248, 0.3);
  --glow-blue: 0 0 12px rgba(138, 180, 248, 0.3);
  --positive-color: #34a853;
  --negative-color: #ea4335;
  --shadow: 0 4px 16px rgba(0, 0, 0, 0.4);
  --font-main: 'Inter', -apple-system, sans-serif;
  --font-mono: 'Poppins', monospace;
  --glass-blur: blur(10px);
}

/* ==========================================
   3. TEMA MARINE (Azul Marino Brillante)
   ========================================== */
[data-theme="marine"] {
  --bg-color: #0b2545;
  /* Bright Navy */
  --card-bg: rgba(22, 48, 85, 0.85);

  --color-accent-primary: #4facfe;
  /* Sky Blue Neon */
  --color-accent-secondary: #00f2fe;
  /* Bright Cyan */
  --neon-color: var(--color-accent-primary);
  --cyan-neon: var(--color-accent-secondary);

  --text-main: #eef2f6;
  --text-secondary: #a9b7c6;
  --border-color: rgba(79, 172, 254, 0.4);
  --input-bg: #133b6b;
  --input-border: rgba(79, 172, 254, 0.3);
  --glow-blue: 0 0 15px rgba(0, 242, 254, 0.3);
  --positive-color: #00e676;
  --negative-color: #ff5252;
  --shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
  --font-main: 'Inter', system-ui, sans-serif;
  --font-mono: 'Poppins', monospace;
  --glass-blur: blur(5px);
}

/* ==========================================
   4. TEMA DARK (Clasico/Gris)
   ========================================== */
[data-theme="dark"] {
  --bg-color: #121212;
  --card-bg: #1e1e1e;

  --color-accent-primary: #bb86fc;
  --color-accent-secondary: #03dac6;
  --neon-color: var(--color-accent-primary);
  --cyan-neon: var(--color-accent-secondary);

  --text-main: #e0e0e0;
  --text-secondary: #a0a0a0;
  --border-color: rgba(255, 255, 255, 0.1);
  --input-bg: #2d2d2d;
  --input-border: rgba(255, 255, 255, 0.1);
  --glow-blue: 0 0 4px rgba(255, 255, 255, 0.1);
  --positive-color: #4caf50;
  --negative-color: #f44336;
  --shadow: 0 4px 12px rgba(0, 0, 0, 0.5);
  --font-main: 'Inter', -apple-system, sans-serif;
  --font-mono: 'Poppins', monospace;
  --glass-blur: blur(0px);
}

/* ==========================================
   3D & GLASSMORPHISM UTILITIES (PREMIUM)
   ========================================== */

.glass-3d {
  background: var(--glass-3d-bg);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid var(--glass-3d-border);
  box-shadow: var(--glass-3d-shadow), var(--bevel-light);
  border-radius: 12px;
  transition: var(--transition-bounce);
}


.taskbar-3d {
  background: rgba(2, 8, 2, 0.4);
  backdrop-filter: blur(25px) saturate(200%);
  -webkit-backdrop-filter: blur(25px) saturate(200%);
  border-bottom: 1px solid var(--neon-color);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.8), 0 5px 15px rgba(0, 255, 157, 0.1);
}

/* ... */
.room-name-capsule,
.room-roi-badge {
  background: rgba(0, 255, 157, 0.05);
  border: 1px solid rgba(0, 255, 157, 0.3);
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3), inset 0 1px 1px rgba(255, 255, 255, 0.1);
  transform: translateZ(10px);
  text-shadow: 0 0 5px var(--neon-color);
}

.element-3d-lift {
  transform-style: preserve-3d;
  perspective: 1200px;
  transition: var(--transition-premium);
}

.element-3d-lift:hover {
  transform: translateY(-12px) rotateX(6deg) rotateY(-2deg) scale(1.03);
  box-shadow: var(--depth-shadow-3d), 0 0 30px rgba(0, 255, 157, 0.2);
}

.glow-neon-3d {
  box-shadow: var(--neon-glow-high);
  text-shadow: 0 0 8px var(--neon-color);
}

/* Specific Social Icon 3D Look */
.social-icon-3d {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44px;
  height: 44px;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 50%;
  /* Circular for a more "button-like" 3D look */
  font-size: 1.2rem;
  color: #fff;
  text-decoration: none;
  transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  position: relative;
  overflow: visible;
  /* Changed to visible for glow export */
  box-shadow:
    0 10px 20px rgba(0, 0, 0, 0.5),
    inset 0 2px 5px rgba(255, 255, 255, 0.1),
    inset 0 -2px 5px rgba(0, 0, 0, 0.3);
  backdrop-filter: blur(8px);
}

.social-icon-3d svg {
  width: 20px;
  height: 20px;
  filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.5));
  transition: all 0.3s ease;
  z-index: 2;
}

.social-icon-3d::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, rgba(255, 255, 255, 0.1) 0%, transparent 60%);
  pointer-events: none;
}

.social-icon-3d:hover {
  transform: translateY(-8px) scale(1.1) rotate(5deg);
  border-color: var(--neon-color);
  color: var(--neon-color);
  box-shadow:
    0 20px 40px rgba(0, 0, 0, 0.6),
    0 0 25px var(--neon-color);
}

.social-icon-3d:hover svg {
  transform: scale(1.1);
  filter: drop-shadow(0 0 8px var(--neon-color));
}

.social-icon-3d i {
  filter: drop-shadow(2px 2px 4px rgba(0, 0, 0, 0.5));
}

/* Premium 3D Bevel for Tables/Widgets */
.bevel-3d {
  border: 1px solid rgba(255, 255, 255, 0.05);
  box-shadow:
    inset 1px 1px 1px rgba(255, 255, 255, 0.1),
    inset -1px -1px 1px rgba(0, 0, 0, 0.4),
    0 10px 20px rgba(0, 0, 0, 0.4);
}

@keyframes float-3d {

  0%,
  100% {
    transform: translateY(0) rotateX(0);
  }

  50% {
    transform: translateY(-10px) rotateX(2deg);
  }
}

.float-3d {
  animation: float-3d 6s ease-in-out infinite;
}


/* TransiciÃ³n suave para elementos clave Ãºnicamente */
button,
input,
select,
.action-btn,
.total-box,
.sala,
.sidebar-header,
.sync-idle {
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

/* FIX INTERACTIVIDAD NEON - REFORCED HOVER STATES WITH 3D LIFT */
button,
.action-btn {
  position: relative;
  overflow: hidden;
  border-radius: 8px;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, transparent 100%);
  border: 1px solid rgba(0, 255, 157, 0.2);
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3), inset 0 1px 1px rgba(255, 255, 255, 0.05);
  transform-style: preserve-3d;
  transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

button:hover,
.action-btn:hover {
  cursor: pointer;
  filter: brightness(1.2);
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.4), 0 0 15px var(--neon-color);
  transform: translateY(-4px) scale(1.05) rotateX(4deg);
  border-color: var(--neon-color);
}

button:active,
.action-btn:active {
  transform: translateY(1px) scale(0.98);
  filter: brightness(0.9);
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.5), 0 0 5px var(--neon-color);
}

/* 3D CHIPS & BADGES */
.room-name-capsule,
.room-roi-badge {
  background: rgba(0, 255, 157, 0.05);
  border: 1px solid rgba(0, 255, 157, 0.3);
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3), inset 0 1px 1px rgba(255, 255, 255, 0.1);
  transform: translateZ(10px);
  text-shadow: 0 0 5px var(--neon-color);
}

input:focus,
select:focus {
  border-color: #00f2ff;
  box-shadow: 0 0 10px rgba(0, 242, 255, 0.3), inset 0 0 5px rgba(0, 242, 255, 0.1);
  background: rgba(0, 18, 48, 0.5);
  outline: none;
}

body {
  margin: 0;
  padding: 0;
  background-color: var(--bg-color);
  color: #fff;
  font-family: var(--font-main);
  height: 100vh;
  overflow: hidden !important;
  /* Font Smoothing & Definition for Cleaner Look */
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
}

/* =========================================
   SCROLLBAR CUSTOMIZATION (PREMIUM NEON)
   ========================================= */

/* WebKit (Chrome, Edge, Safari) */
::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}

::-webkit-scrollbar-track {
  background: rgba(0, 0, 0, 0.3);
  border-left: 1px solid rgba(255, 255, 255, 0.05);
}

::-webkit-scrollbar-thumb {
  background: rgba(0, 255, 157, 0.3);
  border-radius: 3px;
  transition: background 0.3s;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--neon-color);
  box-shadow: 0 0 10px var(--neon-color);
}

/* Firefox */
* {
  scrollbar-width: thin;
  scrollbar-color: rgba(0, 255, 157, 0.3) rgba(0, 0, 0, 0.3);
}

/* Utility to hide scrollbars only where explicitly needed */
.no-scroll {
  scrollbar-width: none !important;
}

.no-scroll::-webkit-scrollbar {
  display: none !important;
}

html {
  overflow: hidden;
  /* Mantener overflow hidden en html para app-like feel, pero permitir scroll interno */
  height: 100%;
  width: 100%;
}

/* ==========================================
   5. DASHBOARD & ROOMS LAYOUT
   ========================================== */

/* HEADER DEL MÃ“DULO */
.room-header {
  padding: 8px 12px;
  background: rgba(0, 0, 0, 0.2);
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid var(--border-color);
}

.room-header h3 {
  margin: 0;
  font-size: 15px;
  /* +8% from 14px */
  color: var(--neon-color);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: flex;
  align-items: center;
  gap: 8px;
}

/* =========================================
   LAYOUT PRINCIPAL
   ========================================= */
#dashboard {
  display: flex;
  flex-direction: column;
  height: 100vh;
}

.top-bar {
  background: #050a05;
  padding: 5px 15px;
  border-bottom: 1px solid var(--border-color);
  display: flex;
  justify-content: space-between;
  align-items: center;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.8);
  height: 40px;
  z-index: 1000;
}

.top-bar h2 {
  margin: 0;
  font-size: 1.1rem;
  color: #00ff00;
  text-transform: uppercase;
  letter-spacing: 2px;
  font-weight: 900;
}

.top-actions {
  display: flex;
  gap: 15px;
  align-items: center;
}

.layout-container {
  display: flex;
  flex: 1;
  overflow: hidden;
  /* Scroll interno en paneles */
}

/* Utility Grid */
.grid-2-col {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
}

/* =========================================
   PANEL IZQUIERDO (MAIN)
   ========================================= */
#dashboard-main {
  flex: 1;
  /* Ocupa todo el espacio restante */
  padding: 2px 5px;
  /* Further reduced top padding */
  overflow-y: auto;
  border-right: 1px solid var(--border-color);
  display: flex;
  flex-direction: column;
  gap: 5px;
  /* Reduced gap between totals and rooms */
  position: relative;
  /* FIX: Asegura contexto de apilamiento */
  z-index: 1;
  /* FIX: Previene superposicion de fantasmas */
}

/* Grid 3 Columns for New Layout */
.grid-3-col {
  display: grid;
  grid-template-columns: 1fr 1fr 0.6fr;
  gap: 3px;
}

/* Totales Container Compactness */
.totales-container {
  display: flex;
  justify-content: stretch;
  gap: 10px;
  margin: 40px 0;
  /* Reduced to move main content higher */
  padding: 0 5px;
}

.total-box {
  background: #020b02;
  border: 1px solid #1e5e1e;
  border-radius: 4px;
  padding: 4px 12px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex: 1;
  min-width: 150px;
  box-shadow: inset 0 0 15px rgba(0, 255, 0, 0.05);
}

.total-box label {
  color: #88cc88;
  font-size: 17px;
  /* +20% from 14px */
  margin: 0;
  white-space: nowrap;
  text-transform: uppercase;
  font-weight: bold;
}

.total-box input {
  background: transparent;
  border: none;
  color: #00ff00;
  font-size: 18px;
  /* Unified size */
  font-weight: bold;
  text-align: right;
  width: 100%;
  font-family: var(--font-mono);
  outline: none;
}

.green-glow {
  border-color: #00ff00;
  box-shadow: 0 0 8px rgba(0, 255, 0, 0.15);
}

/* Salas Container: Grid de Cartas */
/* DISABLED: Conflicts with flexbox layout in zafiro_core.css */
/* #salas-container {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(480px, 1fr));
  gap: 15px;
  padding: 10px;
  align-items: start;
} */
/* Layout is now controlled by zafiro_core.css flexbox system */



.poker-room-modular {
  background: rgba(2, 10, 25, 0.95);
  backdrop-filter: blur(30px) saturate(200%);
  -webkit-backdrop-filter: blur(30px) saturate(200%);
  border: 1px solid rgba(0, 162, 255, 0.3);
  border-radius: 16px;
  padding: 16px;
  box-shadow: 0 30px 60px rgba(0, 0, 0, 0.9), inset 0 0 0 1px rgba(255, 255, 255, 0.03);
  transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
  position: relative;
  overflow: visible;
  height: fit-content;
  /* Width and margin are defined in zafiro_core.css with fixed 500px */
}

.poker-room-modular::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--border-color), transparent);
}

.poker-room-modular:hover {
  border-color: #00f2ff;
  box-shadow: 0 15px 50px rgba(0, 0, 0, 0.8), 0 0 25px rgba(0, 162, 255, 0.2);
  transform: translateY(-2px);
}

.room-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 6px;
  padding-bottom: 4px;
  border-bottom: 1px solid var(--border-color);
}

.room-name-capsule {
  background: linear-gradient(135deg, rgba(0, 162, 255, 0.2), rgba(0, 140, 255, 0.1));
  color: #00f2ff;
  padding: 4px 10px;
  border-radius: 50px;
  font-size: 14.5px;
  font-weight: 700;
  letter-spacing: -0.3px;
  border: 1px solid #00f2ff;
  box-shadow: 0 0 15px rgba(0, 242, 255, 0.3);
  font-family: var(--font-main);
}

.room-roi-badge {
  background: rgba(0, 242, 255, 0.1);
  color: #00f2ff;
  padding: 4px 10px;
  border-radius: 50px;
  font-size: 14.5px;
  font-weight: 600;
  letter-spacing: -0.3px;
  border: 1px solid #00f2ff;
  box-shadow: 0 0 15px rgba(0, 162, 255, 0.2);
}

.header-torneos-box {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.03);
  padding: 2px 8px;
  border-radius: 8px;
  border: 1px solid rgba(255, 255, 255, 0.05);
  min-width: 80px;
}

.header-torneos-label {
  font-size: 8px;
  color: #888;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  line-height: 1;
}

.header-torneos-val {
  font-size: 16px;
  color: #fff;
  font-weight: 800;
  font-family: var(--font-mono);
}

/* SecciÃ³n Header (Encabezados de InversiÃ³n/Retorno) */
.section-header {
  font-size: 13px;
  font-weight: 600;
  color: #00f2ff;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  padding: 5px 0 3px 0;
  margin-top: 6px;
  border-bottom: 1px solid rgba(0, 162, 255, 0.4);
}

/* Tabla de Datos */
.room-table {
  display: flex;
  flex-direction: column;
  gap: 1px;
  margin-bottom: 3px;
}

.table-header-row {
  display: grid;
  grid-template-columns: 85px 90px 105px 95px;
  align-items: center;
  gap: 7px;
  padding: 8px 0;
  border-bottom: 2px solid rgba(0, 162, 255, 0.5);
  margin: 6px 0;
  background: linear-gradient(to right, rgba(0, 162, 255, 0.15), transparent);
}

.col-header {
  font-size: 11.5px;
  font-weight: 900;
  color: #00f2ff;
  text-transform: uppercase;
  letter-spacing: 0.8px;
  text-align: center;
  text-shadow: 0 0 10px rgba(0, 242, 255, 0.5);
}

.table-row {
  display: grid;
  grid-template-columns: 85px 90px 105px 95px;
  align-items: center;
  gap: 7px;
  padding: 8px 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  transition: all 0.2s ease;
  min-height: 48px;
}

.table-row:hover {
  background: rgba(255, 255, 255, 0.02);
}

.table-row label {
  font-size: 11px;
  /* +20% from 9px */
  color: rgba(255, 255, 255, 0.9);
  text-transform: uppercase;
  font-weight: 800;
  letter-spacing: 0.1px;
  text-align: center;
  line-height: 1.1;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  font-family: var(--font-main);
  padding: 0 1px;
}

.table-row input {
  background: rgba(0, 0, 0, 0.4);
  border: 1px solid rgba(0, 255, 157, 0.15);
  border-radius: 8px;
  color: #fff;
  padding: 8px;
  font-size: 18px;
  font-weight: 700;
  text-align: center;
  font-family: var(--font-mono);
  box-shadow: inset 0 2px 10px rgba(0, 0, 0, 0.5), 0 1px 1px rgba(255, 255, 255, 0.05);
  transition: all 0.3s ease;
  width: 100%;
  outline: none;
}

.table-row input:focus {
  border-color: #00ff9d;
  box-shadow: inset 0 2px 10px rgba(0, 0, 0, 1), 0 0 20px rgba(0, 255, 157, 0.4);
  background: #000;
}

.total-cell {
  background: rgba(0, 255, 157, 0.05);
  border: 1px solid rgba(0, 255, 157, 0.15);
  border-radius: 6px;
  color: #00ff9d;
  padding: 6px;
  font-size: 18px;
  /* Unified size */
  font-weight: 800;
  text-align: center;
  font-family: var(--font-mono);
  font-variant-numeric: tabular-nums;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 40px;
  box-shadow: 0 0 15px rgba(0, 255, 157, 0.05);
}

.total-label {
  font-size: 10.2px;
  /* +20% from 8.5px */
  color: rgba(255, 255, 255, 0.5) !important;
  text-transform: uppercase;
  font-weight: 700;
  letter-spacing: 0.5px;
  text-align: center;
  line-height: 1.2;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
  padding: 0 1px;
}

.net-result-yellow {
  background: #ffff00 !important;
  color: #000 !important;
  font-weight: 900 !important;
  text-shadow: none !important;
  border: 1px solid #ccaa00 !important;
  box-shadow: 0 0 12px rgba(255, 255, 0, 0.3);
}

.subtotal-row {
  background: linear-gradient(90deg, rgba(0, 255, 157, 0.05), rgba(0, 255, 157, 0.02));
  padding: 6px 0;
  margin-top: 4px;
  border-top: 1px solid rgba(0, 255, 157, 0.2);
  border-bottom: 1px solid rgba(0, 255, 157, 0.1);
}

.total-row {
  background: linear-gradient(90deg, rgba(255, 255, 255, 0.05), transparent);
  padding: 10px 0;
  margin-top: 8px;
  border-top: 2px solid rgba(255, 255, 255, 0.15);
  border-bottom: 2px solid rgba(255, 255, 255, 0.05);
}

.total-row label {
  font-weight: 800;
  color: #fff;
}

.accent-cyan {
  color: var(--cyan-neon) !important;
}

.accent-green {
  color: var(--neon-color) !important;
}

.text-yellow-vivid {
  color: #ffff00 !important;
  font-weight: bold;
}

.bold {
  font-weight: 900;
  font-size: 13px;
}

.room-section:nth-child(1) .total-display {
  color: var(--cyan-neon);
}

.room-section:nth-child(2) .total-display {
  color: var(--neon-color);
}


.sidebar-header h3 {
  margin: 0 0 10px 0;
  color: var(--text-muted);
  font-size: 14px;
  text-transform: uppercase;
  border-bottom: 1px solid var(--border-color);
  padding-bottom: 5px;
}

.sidebar-actions {
  display: flex;
  gap: 5px;
  margin-bottom: 10px;
}

.action-btn {
  background: var(--card-bg);
  border: 1px solid var(--border-color);
  color: #fff;
  padding: 8px 12px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 12px;
  flex: 1;
  transition: all 0.2s;
}

.action-btn:hover {
  background: #0f2f0f;
  border-color: var(--neon-color);
}

.action-btn.small {
  padding: 5px 10px;
}

/* Historial List Items 3D Upgrade */
.historial-item-styled {
  background: rgba(255, 255, 255, 0.02);
  border: 1px solid rgba(0, 255, 157, 0.1);
  border-radius: 12px;
  padding: 12px;
  margin-bottom: 12px;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  position: relative;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3), inset 0 1px 1px rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(5px);
}

.historial-item-styled:hover {
  background: rgba(255, 255, 255, 0.05);
  border-color: var(--neon-color);
  transform: translateY(-5px) scale(1.02);
  box-shadow: 0 15px 30px rgba(0, 0, 0, 0.5), 0 0 15px rgba(0, 255, 157, 0.2);
}

.historial-item-styled:active {
  transform: translateY(1px) scale(0.98);
}

.historial-date {
  font-family: 'Courier New', Courier, monospace !important;
  font-size: 10px;
  color: #666;
  margin-bottom: 2px;
}

.historial-name {
  font-family: 'Courier New', Courier, monospace !important;
  font-size: 13px;
  font-weight: bold;
  color: #fff;
  margin-bottom: 5px;
  letter-spacing: 0.5px;
}

.historial-row {
  font-family: 'Courier New', Courier, monospace !important;
  display: flex;
  justify-content: space-between;
  font-size: 11px;
  color: #aaa;
  margin-bottom: 2px;
}

.historial-val {
  color: #fff;
  font-family: 'Courier New', Courier, monospace !important;
  font-weight: bold;
}

.historial-divider {
  border-top: 1px dashed #333;
  margin-top: 5px;
  padding-top: 3px;
  text-align: right;
}

.historial-result {
  font-family: 'Courier New', Courier, monospace !important;
  font-size: 14px;
  font-weight: bold;
}

/* Delete Mode Styles */
.delete-mode-checkbox {
  position: absolute;
  top: 10px;
  right: 10px;
  width: 20px;
  height: 20px;
  cursor: pointer;
  z-index: 10;
}

/* =========================================
   LOGIN SCREEN
   ========================================= */
#access-screen {
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  width: 100vw !important;
  height: 100vh !important;
  background: rgba(0, 0, 0, 0.85) !important;
  /* Semi-transparente oscuro elegante */
  display: flex !important;
  justify-content: center !important;
  align-items: center !important;
  z-index: 99999 !important;
  /* Máxima prioridad */
  backdrop-filter: blur(5px);
  /* Efecto cristal */
}

.login-card {
  background: #0a1a0a;
  border: 1px solid var(--neon-color);
  padding: 30px;
  border-radius: 8px;
  width: 300px;
  text-align: center;
  box-shadow: 0 0 20px rgba(0, 255, 0, 0.1);
}

.login-card input {
  width: 100%;
  padding: 10px;
  margin-bottom: 10px;
  background: #020b02;
  border: 1px solid #333;
  color: #fff;
  border-radius: 4px;
  box-sizing: border-box;
}

.login-card button {
  width: 100%;
  padding: 10px;
  background: var(--neon-color);
  color: #000;
  border: none;
  border-radius: 4px;
  font-weight: bold;
  cursor: pointer;
  margin-top: 10px;
}

.login-card button:hover {
  background: #44ff44;
}

/* Password Eye */
.password-wrapper {
  position: relative;
  width: 100%;
}

#password-eye {
  position: absolute;
  right: 10px;
  top: 50%;
  /* Center vertically relative to wrapper */
  transform: translateY(-50%);
  /* Perfect centering */
  background: none;
  border: none;
  cursor: pointer;
  color: #888;
  padding: 0;
  width: 24px;
  height: 24px;
  margin: 0;
  /* Remove default button margins */
  margin-top: -5px;
  /* Slight adjustment for input margin-bottom offset if needed */
}

/* =========================================
   SALA CARD STYLES
   ========================================= */
.sala {
  background: #030f03;
  border: 1px solid #1e5e1e;
  border-radius: 6px;
  margin-bottom: 8px;
  overflow: hidden;
  position: relative;
  padding-bottom: 5px;
}

.sala-header {
  background: #081808;
  padding: 5px 10px;
  border-bottom: 1px solid #1e5e1e;
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 40px;
  /* Altura fija para consistencia */
}

.sala-header-content {
  display: flex;
  align-items: center;
  gap: 10px;
  flex: 1;
}

.sala-logo-container {
  width: 30px;
  height: 30px;
  background: #000;
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  border: 1px solid #333;
}

.sala-logo-img {
  max-width: 100%;
  max-height: 100%;
  object-fit: contain;
}

.sala-title-text {
  font-weight: bold;
  color: #fff;
  font-size: 14px;
}

.sala-remove-btn-styled {
  background: none;
  border: none;
  color: #666;
  font-size: 18px;
  cursor: pointer;
  padding: 0 5px;
  line-height: 1;
}

.sala-remove-btn-styled:hover {
  color: #ff4444;
}

.sala-body {
  padding: 8px;
}

/* Input Group Styles */
.input-group {
  display: flex;
  flex-direction: column;
  margin-bottom: 2px;
}

.input-group label {
  font-size: 10px;
  color: #888;
  margin-bottom: 1px;
}

.input-group input {
  background: #000;
  border: 1px solid #333;
  color: #fff;
  padding: 4px;
  font-size: 13px;
  border-radius: 3px;
  font-family: monospace;
  text-align: right;
}

.input-group input:focus {
  border-color: var(--neon-color);
  outline: none;
}

/* Result Box in Card */
.result-box {
  grid-column: span 3;
  /* Full width in 3-col grid */
  background: #000;
  border: 1px solid #333;
  padding: 5px;
  text-align: center;
  border-radius: 4px;
  margin-top: 5px;
}

.result-box label {
  display: block;
  font-size: 10px;
  color: #aaa;
}

.result-value {
  font-size: 16px;
  font-weight: bold;
  font-family: monospace;
}

/* =========================================
   ALARM STYLES
   ========================================= */
@keyframes ring {
  0% {
    transform: rotate(0);
  }

  10% {
    transform: rotate(30deg);
  }

  20% {
    transform: rotate(-28deg);
  }

  30% {
    transform: rotate(34deg);
  }

  40% {
    transform: rotate(-32deg);
  }

  50% {
    transform: rotate(30deg);
  }

  60% {
    transform: rotate(-28deg);
  }

  70% {
    transform: rotate(34deg);
  }

  80% {
    transform: rotate(-32deg);
  }

  90% {
    transform: rotate(30deg);
  }

  100% {
    transform: rotate(0);
  }
}

.alarm-ringing {
  animation: ring 1s infinite;
  color: #ff3333 !important;
}

/* =========================================
   UTILITIES
   ========================================= */
.hidden {
  display: none !important;
}

.active {
  display: block;
}

/* Modal Custom Styles */
.custom-modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.85);
  z-index: 50000;
  display: flex;
  justify-content: center;
  align-items: center;
  opacity: 0;
  visibility: hidden;
  transition: all 0.2s ease-in-out;
}

.custom-modal-overlay.active {
  opacity: 1;
  visibility: visible;
}

.custom-modal-box {
  background: #050a05;
  border: 1px solid var(--neon-color);
  padding: 30px;
  border-radius: 12px;
  width: 95%;
  max-width: 450px;
  box-shadow: 0 0 50px rgba(0, 0, 0, 0.9), 0 0 20px rgba(0, 255, 157, 0.1);
  transform: scale(0.9);
  transition: all 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
  position: relative;
}

.custom-modal-overlay.active .custom-modal-box {
  transform: scale(1);
}

.custom-modal-title {
  margin: 0 0 20px 0;
  color: var(--neon-color);
  font-size: 20px;
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  border-bottom: 1px solid rgba(0, 255, 157, 0.2);
  padding-bottom: 15px;
}

.custom-modal-message {
  color: #fff;
  margin-bottom: 20px;
  line-height: 1.5;
  font-size: 14px;
}

.custom-modal-input {
  width: 100%;
  padding: 10px;
  background: #111;
  border: 1px solid #1e5e1e;
  color: #fff;
  border-radius: 4px;
  margin-bottom: 20px;
  box-sizing: border-box;
  outline: none;
}

.custom-modal-input:focus {
  border-color: var(--cyan-neon);
}

/* Fix for calendar indicator in dark mode */
input[type="datetime-local"]::-webkit-calendar-picker-indicator {
  filter: invert(1) hue-rotate(180deg);
  cursor: pointer;
  width: 28px;
  height: 28px;
  padding: 5px;
  border-radius: 4px;
  transition: background 0.2s;
}

input[type="datetime-local"]::-webkit-calendar-picker-indicator:hover {
  background: rgba(0, 242, 255, 0.1);
}

/* Ensure the input looks like a big clickable area */
input[type="datetime-local"] {
  font-size: 18px !important;
  font-weight: bold;
  cursor: pointer;
  letter-spacing: 1px;
}

.custom-modal-actions {
  display: flex;
  justify-content: flex-end;
  gap: 15px;
  margin-top: 10px;
}

.modal-btn {
  padding: 10px 20px;
  border-radius: 6px;
  cursor: pointer;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-size: 12px;
  border: none;
  transition: all 0.2s;
}

.modal-btn.primary {
  background: linear-gradient(135deg, #00f2ff, #00d2ff);
  color: #001230;
  box-shadow: 0 4px 12px rgba(0, 242, 255, 0.2);
}

.modal-btn.primary:hover {
  background: linear-gradient(135deg, #33f8ff, #00f2ff);
  transform: translateY(-1px);
  box-shadow: 0 6px 15px rgba(0, 242, 255, 0.3);
}

.modal-btn.secondary {
  background: #222;
  color: #eee;
  border: 1px solid #444;
}

.modal-btn.secondary:hover {
  background: #333;
  border-color: #666;
}

/* BotÃ³n de Ajustes - Fix Z-Index y Posicionamiento */
#settings-btn {
  position: relative;
  z-index: 2147483647 !important;
  /* Absolute max value */
  cursor: pointer;
  padding: 8px !important;
  /* Reduced from 15px to prevent content clipping */
  width: auto !important;
  /* Override fixed width from .tb-tool-btn */
  min-width: 40px;
  background: transparent !important;
  border-radius: 50%;
  transition: transform 0.2s;
  pointer-events: auto !important;
  /* Force enable */
  overflow: visible !important;
  /* Fix: Prevent gear icon from being clipped */
}

#settings-btn svg {
  pointer-events: none;
  /* Click pass-through */
  display: block;
}

#settings-btn:hover {
  transform: rotate(30deg) scale(1.1);
}


/* SETTINGS MODAL - SIDEBAR LAYOUT */
/* SETTINGS MODAL - DROPDOWN LAYOUT (Refactored) */
#settings-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: rgba(0, 0, 0, 0.85);
  z-index: 12000;
  /* Above Taskbar (10000) */
  display: none;
  /* Default hidden */
  justify-content: center;
  align-items: center;
  flex-direction: column;
  backdrop-filter: blur(5px);
}

#settings-modal.flex-display {
  display: flex !important;
}

.flex-display {
  display: flex !important;
  justify-content: center !important;
  align-items: center !important;
}

#room-selector-modal {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 2500;
  display: flex;
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(4px);
  justify-content: center;
  align-items: center;
}

#room-selector-modal {
  justify-content: center;
  align-items: center;
}

.hidden {
  display: none !important;
}

.settings-modal-content {
  width: 460px;
  max-width: 90vw;
  height: auto;
  max-height: 80vh;
  margin: 0;
  display: flex;
  flex-direction: column;
  background: rgba(0, 18, 48, 0.98);
  border: 1px solid var(--cyan-neon);
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.8);
  border-radius: 8px;
  /* Rounded corners */
  border-left: 1px solid rgba(0, 242, 255, 0.2);
  padding: 0;
  box-shadow: -5px 0 30px rgba(0, 0, 0, 0.8);
  animation: slideInRight 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  box-sizing: border-box;
  /* Asegurar que bordes no sumen ancho */
}

.room-selector-window {
  width: 310px;
  /* Reduced from 380px (approx 20%) */
  max-width: 95vw;
  height: auto;
  min-height: 400px;
  /* Reduced from 480px (approx 20%) */
  max-height: 90vh;
  background: rgba(0, 18, 48, 0.95);
  border: 1px solid #00f2ff;
  border-radius: 12px;
  display: flex;
  flex-direction: column;
  padding: 0;
  box-shadow: 0 0 60px rgba(0, 0, 0, 0.9), 0 0 25px rgba(0, 242, 255, 0.15);
  animation: modalPop 0.35s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  overflow: hidden;
  box-sizing: border-box;
}

@keyframes modalPop {
  0% {
    transform: scale(0.9);
    opacity: 0;
  }

  100% {
    transform: scale(1);
    opacity: 1;
  }
}

.room-selector-window .settings-header {
  padding: 20px 25px 15px 25px;
  border-bottom: 1px dashed rgba(0, 242, 255, 0.2);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.room-selector-window .settings-body {
  padding: 25px !important;
  flex: 1;
  overflow-y: auto;
}

@keyframes slideInRight {
  from {
    transform: translateX(100%);
    opacity: 0;
  }

  to {
    transform: translateX(0);
    opacity: 1;
  }
}

.settings-header {
  padding: 15px;
  border-bottom: 1px solid #1e5e1e;
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: #020b02;
}

.settings-header h3 {
  margin: 0;
  color: var(--neon-color);
}

.settings-tabs {
  display: flex;
  background: #000;
  border-bottom: 1px solid #1e5e1e;
  overflow-x: auto;
  white-space: nowrap;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: thin;
}

.tab-btn {
  flex: 0 0 auto;
  padding: 15px;
  background: none;
  border: none;
  color: #666;
  cursor: pointer;
  border-bottom: 2px solid transparent;
  transition: all 0.2s;
}

.tab-btn.active {
  color: #fff;
  border-bottom-color: var(--neon-color);
  background: #0a1a0a;
}

.settings-body {
  flex: 1;
  overflow-y: auto;
  padding: 20px;
}

.tab-content {
  display: none !important;
}

.tab-content.active {
  display: block !important;
  animation: fadeIn 0.3s;
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

/* Ajustes especÃ­ficos para el calendario compacto */
#calendar-grid div {
  font-size: 11px !important;
  /* Aumentado de 9px */
  padding: 1px !important;
  position: relative;
  /* Para los marcadores */
}

.calendar-wrapper {
  transform: scale(0.9);
  /* Ligeramente mÃ¡s grande */
  transform-origin: top left;
  margin-bottom: 0 !important;
  width: 110%;
}

/* Marcadores de Backup */
.backup-marker {
  position: absolute;
  bottom: 1px;
  left: 50%;
  transform: translateX(-50%);
  width: 3px;
  height: 3px;
  border-radius: 50%;
  display: block;
}

.marker-create {
  background-color: #aa0000;
}

.marker-restore {
  background-color: #ff5555;
}

.marker-full {
  background-color: #ffffff;
}

/* Modal Search Input */
.modal-search-input {
  width: 100%;
  padding: 10px;
  background: #001230;
  border: 1px solid #004d60;
  color: #fff;
  margin-bottom: 10px;
  box-sizing: border-box;
  border-radius: 4px;
}

.modal-room-select {
  width: 100%;
  padding: 10px;
  background: #001230;
  border: 1px solid #004d60;
  color: #fff;
  margin-bottom: 15px;
  border-radius: 4px;
}

/* =========================================
   UTILIDADES VISUALES (Theme Aware)
   ========================================= */
.accent-cyan {
  color: var(--cyan-neon) !important;
}

.accent-green {
  color: var(--positive-color) !important;
}

.accent-red {
  color: var(--negative-color) !important;
}

.bold {
  font-weight: 700 !important;
}

.total-cell {
  font-family: var(--font-mono);
  text-align: right;
  font-feature-settings: "tnum";
}

/* =========================================
   RESPONSIVE DESIGN SYSTEM
   ========================================= */
/* Tablet & Mobile Landscape */
@media (max-width: 1024px) {
  .layout-container {
    flex-direction: column;
  }

  #historial-sidebar {
    width: 100%;
    height: auto;
    max-height: 300px;
    border-left: none;
    border-top: 1px solid var(--border-color);
  }

  #dashboard-main {
    width: 100%;
  }

  /* Ajuste de Grid para Tablet */
  #salas-container {
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 10px;
  }
}

/* Mobile Portrait */
@media (max-width: 768px) {
  .header-actions button span {
    display: none;
    /* Ocultar texto de botones en header */
  }

  .totales-container {
    flex-direction: column;
    gap: 10px;
  }

  .total-box {
    width: 100%;
  }

  #salas-container {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .poker-room-modular {
    width: 100%;
    max-width: 100%;
    /* Full width en mÃ³vil */
  }

  /* Ajuste de fuentes para mÃ³vil */
  .room-header h3 {
    font-size: 14px;
  }

  .table-row input {
    font-size: 16px;
    /* Evitar zoom en iOS */
  }
}

/* Small Mobile */
@media (max-width: 480px) {
  .settings-modal-content {
    width: 95%;
    margin: 10px auto;
  }
}


.modal-section-label {
  color: #00d2ff;
  font-size: 12px;
  display: block;
  margin-bottom: 8px;
}

.modal-radio-group {
  display: flex;
  gap: 15px;
  margin-bottom: 20px;
}

.modal-radio-label {
  cursor: pointer;
  font-size: 14px;
  display: flex;
  align-items: center;
  gap: 5px;
}

.modal-radio-label.cash {
  color: #aaf;
}

.modal-radio-label.mtt {
  color: #faa;
}

/* AnimaciÃ³n MicrÃ³fono de Voz */
.pulse-animation {
  animation: pulse-red 1.5s infinite ease-in-out;
  text-shadow: 0 0 10px rgba(255, 0, 0, 0.5);
}

@keyframes pulse-red {
  0% {
    transform: scale(1);
    opacity: 1;
  }

  50% {
    transform: scale(1.2);
    opacity: 0.7;
  }

  100% {
    transform: scale(1);
    opacity: 1;
  }
}

/* =========================================
   SIMULACIÃ“N RESPONSIVE (ADMIN)
   ========================================= */
body.sim-tablet .layout-container {
  flex-direction: column;
}

body.sim-tablet #historial-sidebar {
  width: 100%;
  height: auto;
  max-height: 300px;
  border-left: none;
  border-top: 1px solid var(--border-color);
}

body.sim-tablet #dashboard-main {
  width: 100%;
}

body.sim-tablet #salas-container {
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 10px;
}

body.sim-tablet,
body.sim-mobile,
body.sim-fhd {
  max-width: 1024px;
  margin: 0 auto;
  border-left: 1px solid var(--border-color);
  border-right: 1px solid var(--border-color);
  transition: max-width 0.3s ease;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
  /* Visual depth for generic sim */
}

/* Fuerza FHD (1920x1080 16:9) */
body.sim-fhd {
  max-width: 1920px;
  aspect-ratio: 16 / 9;
  border-bottom: 1px solid var(--border-color);
  box-shadow: 0 0 50px rgba(0, 0, 0, 0.8);
  margin-top: 10px;
  margin-bottom: 10px;
  overflow-y: auto;
  /* Allow scroll inside the 16:9 container if needed, or behave like screen */
}

/* Fuerza Mobile */
body.sim-mobile {
  max-width: 480px;
}

body.sim-mobile .layout-container {
  flex-direction: column;
}

body.sim-mobile .header-actions button span {
  display: none;
}

body.sim-mobile .totales-container {
  flex-direction: column;
  gap: 10px;
}

body.sim-mobile .total-box {
  width: 100%;
}

/* RE-ADJUST DASHBOARD PADDING */
#dashboard {
  padding-top: 60px;
}

body.sim-mobile #salas-container {
  display: flex;
  flex-direction: column;
  align-items: center;
}

body.sim-mobile .poker-room-modular {
  width: 96%;
  margin: 5px auto;
  border-radius: 8px;
}

.modal-radio-label.mixed {
  color: #faf;
}

/* AnimaciÃ³n de pulso para micrÃ³fono */
@keyframes pulse-red {
  0% {
    transform: scale(1);
    filter: drop-shadow(0 0 2px rgba(255, 0, 0, 0.5));
  }

  50% {
    transform: scale(1.1);
    filter: drop-shadow(0 0 10px rgba(255, 0, 0, 0.8));
  }

  100% {
    transform: scale(1);
    filter: drop-shadow(0 0 2px rgba(255, 0, 0, 0.5));
  }
}

.pulse-animation {
  animation: pulse-red 1.2s infinite ease-in-out;
}

/* Animaciones para TranscripciÃ³n de Voz */
@keyframes fadeInBubble {
  from {
    opacity: 0;
    transform: translateY(10px) scale(0.95);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.chat-bubble-live {
  position: relative;
  transition: all 0.2s ease;
  background: #1e2a3a !important;
  /* MÃ¡s contraste */
  border: 1px solid var(--cyan-neon) !important;
  color: #fff !important;
  font-weight: 500;
}

.chat-bubble-live::after {
  content: '';
  position: absolute;
  left: -8px;
  bottom: 0;
  width: 0;
  height: 0;
  border-left: 10px solid transparent;
  border-right: 10px solid transparent;
  border-top: 10px solid #1e2a3a;
  transform: rotate(45deg);
}

/* Puntos suspensivos animados para el "Escuchando" */
.listening-dots::after {
  content: '...';
  display: inline-block;
  width: 1.5em;
  text-align: left;
  animation: dots 1.5s steps(4, end) infinite;
}

@keyframes dots {

  0%,
  20% {
    content: '';
  }

  40% {
    content: '.';
  }

  60% {
    content: '..';
  }

  80% {
    content: '...';
  }
}

/* ==========================================
   ANALYTICS MODULE OVERLAY STYLES
   ========================================== */
#analytics-module-overlay {
  transition: opacity 0.4s ease, visibility 0.4s;
}

#analytics-module-overlay.hidden {
  opacity: 0;
  visibility: hidden;
  display: none !important;
}

.analytics-tab-btn {
  border: 1px solid transparent;
}

.analytics-tab-btn:hover {
  background: rgba(0, 255, 157, 0.05) !important;
  color: var(--cyan-neon) !important;
}

.analytics-tab-btn.active {
  background: rgba(0, 255, 157, 0.1) !important;
  border: 1px solid rgba(0, 255, 157, 0.2) !important;
  color: var(--cyan-neon) !important;
}

.analytics-sub-view {
  animation: fadeIn 0.5s ease-out;
}

@keyframes fadeInOverlay {
  from {
    opacity: 0;
    transform: translateY(10px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* ==========================================
   VOICE MONITOR PANEL (Audit Terminal)
   ========================================== */
#voice-monitor-panel {
  position: fixed;
  bottom: 20px;
  right: 20px;
  width: 320px;
  background: rgba(5, 10, 5, 0.85);
  backdrop-filter: blur(15px);
  border: 1px solid rgba(0, 255, 157, 0.2);
  border-radius: 12px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
  z-index: 9999;
  font-family: var(--font-mono);
  transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  overflow: hidden;
}

#voice-monitor-panel.minimized {
  width: 220px;
  height: 40px;
  transform: translateY(10px);
  opacity: 0.8;
}

.monitor-header {
  background: rgba(0, 255, 157, 0.05);
  padding: 10px 15px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid rgba(0, 255, 157, 0.1);
  cursor: pointer;
}

.pulse-dot {
  width: 6px;
  height: 6px;
  background: var(--cyan-neon);
  border-radius: 50%;
  box-shadow: 0 0 10px var(--cyan-neon);
  animation: monitor-pulse 1.5s infinite;
}

@keyframes monitor-pulse {
  0% {
    transform: scale(1);
    opacity: 1;
  }

  50% {
    transform: scale(1.5);
    opacity: 0.5;
  }

  100% {
    transform: scale(1);
    opacity: 1;
  }
}

#voice-monitor-content {
  height: 180px;
  padding: 15px;
  overflow-y: auto;
  font-size: 11px;
  scrollbar-width: thin;
  scrollbar-color: var(--cyan-neon) transparent;
}

#voice-monitor-content::-webkit-scrollbar {
  width: 4px;
}

#voice-monitor-content::-webkit-scrollbar-thumb {
  background: var(--cyan-neon);
  border-radius: 10px;
}

.monitor-entry {
  margin-bottom: 12px;
  border-left: 2px solid rgba(0, 255, 157, 0.3);
  padding-left: 10px;
  animation: entry-slide 0.3s ease-out;
}

@keyframes entry-slide {
  from {
    opacity: 0;
    transform: translateX(10px);
  }

  to {
    opacity: 1;
    transform: translateX(0);
  }
}

#minimize-monitor-btn {
  background: none;
  border: none;
  color: #555;
  font-size: 18px;
  cursor: pointer;
  transition: color 0.3s;
}

#minimize-monitor-btn:hover {
  color: #ff3333;
}

/* ==========================================
   SYNC STATUS INDICATORS (Firebase)
   ========================================== */
#sync-status-icon {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  display: inline-block;
  transition: all 0.3s ease;
  margin-right: 2px;
}

.sync-idle {
  background: #444;
  box-shadow: 0 0 5px rgba(0, 0, 0, 0.5);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.sync-loading {
  background: var(--cyan-neon);
  box-shadow: 0 0 10px var(--cyan-neon);
  animation: sync-pulse 1s infinite alternate;
}

.sync-success {
  background: #00ff9d;
  box-shadow: 0 0 10px #00ff9d;
}

.sync-error {
  background: #ff3333;
  box-shadow: 0 0 10px #ff3333;
}

@keyframes sync-pulse {
  from {
    transform: scale(0.8);
    opacity: 0.5;
  }

  to {
    transform: scale(1.2);
    opacity: 1;
  }
}

/* ======================================================= */
/* FIX: CUSTOM SELECT V2 (HISTORY TAB) - REPAIRED LAYOUT   */
/* ======================================================= */
.custom-select-v2 {
  position: relative;
  width: 100%;
  user-select: none;
}

.custom-select-v2 .trigger {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  gap: 8px;
  /* Espacio entre lupa y texto */
  width: 100%;
  height: 35px;
  /* Altura fija para alineaciÃ³n */
  padding: 0 10px;
  background: #020b02;
  border: 1px solid #1e5e1e;
  border-radius: 4px;
  color: #fff;
  font-size: 12px;
  cursor: pointer;
  box-sizing: border-box;
  transition: border-color 0.2s;
}

.custom-select-v2 .trigger:hover {
  border-color: #00ff00;
}

.custom-select-v2 .trigger .lupa {
  font-size: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0;
  padding: 0;
  line-height: 1;
  /* Reset line-height to prevent vertical shift */
}

.custom-select-v2 .trigger .text {
  flex: 1;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  text-align: left;
}

.custom-select-v2 .trigger .arrow {
  margin-left: auto;
  color: #88cc88;
  font-size: 10px;
}

.custom-select-v2 .dropdown {
  position: absolute;
  top: 100%;
  left: 0;
  width: 100%;
  background: #0a1a0a;
  border: 1px solid #1e5e1e;
  border-radius: 4px;
  z-index: 999;
  margin-top: 2px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.8);
  display: flex;
  flex-direction: column;
}

.custom-select-v2 .dropdown.hidden {
  display: none !important;
}

.custom-select-v2 .search-wrapper {
  padding: 8px;
  border-bottom: 1px solid #1e5e1e;
  background: #000;
}

.custom-select-v2 .search-wrapper input {
  width: 100%;
  padding: 6px;
  background: #111;
  border: 1px solid #333;
  color: #fff;
  border-radius: 3px;
  font-size: 11px;
  box-sizing: border-box;
  outline: none;
}

.custom-select-v2 .options {
  max-height: 200px;
  overflow-y: auto;
}

.custom-select-v2 .option {
  padding: 8px 10px;
  cursor: pointer;
  font-size: 11px;
  color: #ccc;
  border-bottom: 1px solid #111;
  display: flex;
  align-items: center;
  gap: 8px;
}

.custom-select-v2 .option:hover {
  background: #0f2f0f;
  color: #fff;
}

.custom-select-v2 .option img {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  object-fit: cover;
}

/* =========================================
   MODO STREAMER (ORBIT IDENTITY) - PROTECTION LAYER
   ========================================= */

/* Activation class on BODY */
body.streamer-mode {
  --sensitive-blur: 8px;
  --stream-ui-border: #ff0055;
}

/* Indicador visual de que estamos EN AIRE */
body.streamer-mode::before {
  content: 'âš¡ ORBIT STREAM MODE';
  position: fixed;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  background: var(--stream-ui-border);
  color: white;
  padding: 2px 10px;
  font-size: 9px;
  font-weight: 900;
  letter-spacing: 1px;
  z-index: 99999;
  border-radius: 0 0 10px 10px;
  pointer-events: none;
  box-shadow: 0 0 20px rgba(255, 0, 85, 0.6);
  animation: stream-blink 2s infinite;
}

@keyframes stream-blink {

  0%,
  100% {
    opacity: 1;
  }

  50% {
    opacity: 0.5;
  }
}

/* PROTECCIÃ“N DE DATOS SENSIBLES */
body.streamer-mode .sensitive-data {
  filter: blur(var(--sensitive-blur));
  transition: all 0.3s ease;
  user-select: none;
  cursor: not-allowed;
  position: relative;
  display: inline-block;
}

/* "Peek" feature: Hover para ver (solo para ti) */
body.streamer-mode .sensitive-data:hover {
  filter: blur(0);
  cursor: cell;
}

/* Badge de seguridad sobre datos ocultos */
body.streamer-mode .sensitive-data::after {
  content: 'ðŸ”’';
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 10px;
  opacity: 0.7;
  pointer-events: none;
  text-shadow: 0 0 5px black;
}

body.streamer-mode .sensitive-data:hover::after {
  display: none;
}

/* =========================================
   ORBIT BRANDING ELEMENTS
   ========================================= */
.orbit-logo-text {
  font-family: 'Outfit', sans-serif;
  letter-spacing: 4px;
  text-transform: uppercase;
  background: linear-gradient(90deg, #fff, var(--cyan-neon));
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  font-weight: 900;
  text-shadow: 0 0 20px rgba(0, 255, 157, 0.4);
}

/* EFECTOS DINÃMICOS MODO STREAMER */
body.streamer-mode .glass-panel,
body.streamer-mode .total-box {
  animation: stream-pulse 3s infinite ease-in-out;
  border-color: rgba(255, 0, 85, 0.3) !important;
}

@keyframes stream-pulse {

  0%,
  100% {
    box-shadow: 0 0 10px rgba(255, 0, 85, 0.1);
  }

  50% {
    box-shadow: 0 0 20px rgba(255, 0, 85, 0.3);
  }
}

/* RESTORED VISUAL EFFECTS */
.hover-glow:hover {
  box-shadow: 0 0 15px var(--neon-color);
  border-color: var(--neon-color);
  transform: translateY(-1px);
}

.neon-pulse {
  animation: neonPulse 2s infinite;
}

@keyframes neonPulse {
  0% {
    box-shadow: 0 0 5px var(--neon-color);
  }

  50% {
    box-shadow: 0 0 20px var(--neon-color);
  }

  100% {
    box-shadow: 0 0 5px var(--neon-color);
  }
}

.active-neon {
  background: rgba(0, 255, 157, 0.15) !important;
  border-color: var(--neon-color) !important;
  color: var(--neon-color) !important;
  box-shadow: 0 0 10px rgba(0, 255, 157, 0.3);
}

/* =========================================
   VERTICAL STACK STRATEGY (COLUMN 1)
   ========================================= */
.concept-stack {
  display: flex;
  flex-direction: column;
  align-items: center;
  /* Centrado total */
  justify-content: center;
  gap: 2px;
  /* PequeÃ±a separaciÃ³n vertical */
  width: 100%;
  height: 100%;
}

.compact-label {
  line-height: 1.2;
  margin-bottom: 2px;
  color: var(--cyan-neon) !important;
  /* UnificaciÃ³n Cian/NeÃ³n para tÃ­tulos de conceptos */
  text-transform: uppercase;
  font-weight: bold;
  font-size: 10px;
  /* Label compacto */
}

.mini-counter {
  width: 100% !important;
  /* Adaptable al contenedor */
  max-width: 50px;
  text-align: center;
  border: 1px solid rgba(0, 255, 157, 0.2);
  /* Borde sutil cian */
  background: rgba(0, 0, 0, 0.5);
  color: #ffffff !important;
  /* CORRECCIÃ“N: NÃºmeros en BLANCO */
  font-weight: bold;
  font-family: var(--font-mono);
  padding: 4px 2px;
  border-radius: 4px;
  font-size: 12px;
  /* Altura ajustada */
  height: 24px;
}

.mini-counter:focus {
  border-color: var(--cyan-neon);
  box-shadow: 0 0 5px var(--cyan-neon);
  outline: none;
}

/* Ocultar flechas (spin-buttons) */
.mini-counter::-webkit-outer-spin-button,
.mini-counter::-webkit-inner-spin-button {
  -webkit-appearance: none;
  appearance: none;
  margin: 0;
}

.room-table * {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif !important;
  font-weight: 700 !important;
  text-transform: uppercase !important;
  letter-spacing: 0.5px !important;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-shadow: 0 1px 2px rgba(0, 0, 0, 0.5);
}

.compact-label {
  color: var(--cyan-neon) !important;
  font-size: 10px !important;
  opacity: 1 !important;
}

/* ZAFIRO TOTAL (#00d2ff):
   1. color: #00d2ff !important -> Pinta el número que escribes.
   2. -webkit-text-fill-color -> Fuerza el relleno del texto en Webkit (Chrome).
   3. ::placeholder -> Fuerza el color si el número es un placeholder. */
input[id*="-acc-"] {
  color: #00d2ff !important;
  -webkit-text-fill-color: #00d2ff !important;
  height: 38px !important;
  font-size: 18px !important;
  background: transparent !important;
  border: 1px solid rgba(0, 210, 255, 0.3) !important;
}

/* Forzar color Zafiro en el placeholder también */
input[id*="-acc-"]::placeholder {
  color: #00d2ff !important;
  opacity: 1 !important;
  -webkit-text-fill-color: #00d2ff !important;
}

input:not([id*="-acc-"]) {
  color: #ffffff !important;
  height: 38px !important;
  font-size: 18px !important;
}

.total-cell {
  font-size: 18px !important;
}

/* Close .room-table */

/* =========================================================
   RELEASED STYLES (Previously accidentally nested)
   ========================================================= */

#historial-sidebar {
  position: fixed;
  right: 0;
  bottom: 0;
  top: auto;
  width: 320px;
  height: 75vh !important;
  /* Force override */
  background: rgba(0, 18, 48, 0.98);
  border-top: 1px solid #00f2ff !important;
  border-left: 1px solid rgba(0, 242, 255, 0.2) !important;
  z-index: var(--z-debug);
  overflow-y: auto;
  backdrop-filter: blur(10px);
  box-shadow: -5px -5px 20px rgba(0, 0, 0, 0.5);
  transition: transform 0.3s ease;
  border-top-left-radius: 10px;
}

#historial-sidebar .sidebar-header {
  background: rgba(0, 255, 157, 0.1);
  padding: 10px;
  border-bottom: 1px solid rgba(0, 255, 157, 0.2);
}

#historial-sidebar h3 {
  margin: 0;
  font-family: 'Courier New', Courier, monospace !important;
  font-size: 14px;
  color: var(--cyan-neon);
  text-transform: uppercase;
  letter-spacing: 2px;
  font-weight: bold;
}

.tb-social-btn {
  opacity: 0.7;
  transition: all 0.2s ease;
  display: flex;
  align-items: center;
  justify-content: center;
}

.tb-social-btn:hover {
  opacity: 1;
  transform: scale(1.15);
  filter: drop-shadow(0 0 8px currentColor);
}

/* ================================================== 
   append_bi_styles.css
   Estilos para el mÃ³dulo de Inteligencia Avanzada
   ================================================== */

.bi-kpi-card {
  background: rgba(0, 0, 0, 0.3);
  border: 1px solid rgba(255, 255, 255, 0.05);
  border-radius: 8px;
  padding: 15px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.bi-kpi-label {
  color: #888;
  font-size: 10px;
  text-transform: uppercase;
  font-weight: 700;
}

.bi-kpi-val {
  color: #fff;
  font-size: 20px;
  font-weight: 700;
  margin: 5px 0;
}

.bi-kpi-trend {
  color: #666;
  font-size: 10px;
}

.bi-tag {
  background: #111;
  border: 1px solid #333;
  color: #888;
  padding: 5px 10px;
  border-radius: 20px;
  font-size: 10px;
  cursor: pointer;
  transition: all 0.2s;
}

.bi-tag.active {
  background: rgba(0, 255, 157, 0.1);
  border-color: var(--neon-color);
  color: var(--neon-color);
}

.bi-tag:hover {
  border-color: #666;
  color: #ccc;
}

/* =========================================
   LAB CORRECTIVE PATCH: MARGIN & TOP ALIGNMENT
   ========================================= */

/* 1. Global Shift Up */
#dashboard {
  padding-top: 53px !important;
  /* Spacing for taskbar */
  margin-top: 0 !important;
  position: relative;
  height: 100vh;
  display: flex;
  flex-direction: column;
}

/* 2. Top Bar Force Position */
.app-shell-taskbar {
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  width: 100% !important;
  height: 53px !important;
  z-index: 99999 !important;
  /* Above everything */
  background: rgba(0, 5, 0, 0.95) !important;
  display: flex !important;
  visibility: visible !important;
}

/* 3. Force Sidebar Correctness - FINAL FIX */
#historial-sidebar {
  font-family: 'Courier New', Courier, monospace !important;
  width: 211px !important;
  min-width: 211px !important;
  height: 1085px !important;
  top: auto !important;
  right: 0 !important;
  bottom: 0 !important;
  position: fixed !important;
  /* display: flex !important; REMOVED FOR TOGGLE LOGIC */
  /* visibility: visible !important; REMOVED FOR TOGGLE LOGIC */
  /* opacity: 1 !important; REMOVED FOR TOGGLE LOGIC */
  z-index: var(--z-debug) !important;
  border-left: 1px solid rgba(0, 242, 255, 0.3) !important;
  background: rgba(0, 12, 30, 0.9) !important;
  backdrop-filter: blur(25px) saturate(180%) !important;
  -webkit-backdrop-filter: blur(25px) saturate(180%) !important;
  box-shadow: -10px 0 30px rgba(0, 0, 0, 0.5) !important;
  border-bottom-left-radius: 10px;
}

/* FIX: Ensure Sidebar Children Stack Correctly */
#historial-sidebar>* {
  font-family: inherit !important;
  width: 100% !important;
  max-width: 100% !important;
  flex: 0 0 auto !important;
  display: flex !important;
  flex-direction: column !important;
}

#historial-sidebar .action-btn.small {
  font-family: 'Courier New', Courier, monospace !important;
  font-weight: bold;
  letter-spacing: 0.5px;
}

.history-summary {
  font-family: 'Courier New', Courier, monospace !important;
  font-size: 12px;
  letter-spacing: 0.5px;
}

#historial-sesiones-list {
  flex: 1 1 auto !important;
  overflow-y: auto !important;
  width: 100% !important;
}

/* 4. Layout integrity */
.layout-container {
  display: flex !important;
  flex-direction: row !important;
  flex: 1 !important;
  height: calc(100vh - 53px) !important;
  overflow: hidden !important;
  align-items: stretch !important;
}

#dashboard-main {
  flex: 1 !important;
  overflow-y: auto !important;
  height: 100% !important;
}

/* 5. Force Widgets/Buttons Visibility */
#theme-toggle,
#settings-btn,
#open-lobby-btn,
.tb-dock-btn,
.tb-widget {
  display: flex !important;
  visibility: visible !important;
  opacity: 1 !important;
  z-index: 100001 !important;
}

/* ==========================================
   USER REQUEST: MAX 6 TABLES PER ROW (DASHBOARD GRID)
   ========================================== */
#salas-container {
  display: grid !important;
  grid-template-columns: repeat(5, 1fr) !important;
  gap: 10px !important;
  padding: 10px !important;
  flex-wrap: nowrap !important;
  justify-content: start !important;
}

/* Ensure modular rooms fit the grid cells perfectly */
/* REMOVED: Conflicting .poker-room-modular width rule
   This was forcing rooms to 100% width and conflicting with
   the fixed 500px width defined in zafiro_core.css.
   The zafiro_core.css file is now the single source of truth
   for room dimensions. */

/* ==========================================
   6. PERFORMANCE OPTIMIZATIONS (GPU SAVER)
   ========================================== */
@media (prefers-reduced-motion: reduce) {

  *,
  ::before,
  ::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }

  .glass-3d,
  .taskbar-3d,
  .poker-room-modular,
  #historial-sidebar,
  .room-name-capsule,
  .room-roi-badge {
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
    box-shadow: none !important;
    border: 1px solid var(--border-color) !important;
    background: var(--card-bg) !important;
  }

  /* Specific fix for dashboard main scroll performance */
  #dashboard-main {
    backdrop-filter: none !important;
    transform: translateZ(0);
    /* Force GPU layer without blur expenses */
  }
}

/* =========================================
   6. ORBIT BRANDING: SPLASH & WATERMARK
   ========================================= */

#orbit-splash {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: radial-gradient(circle at center, #0a1220 0%, #02050a 100%);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 100000;
  /* Above everything */
  transition: opacity 1.5s cubic-bezier(0.4, 0, 0.2, 1);
  pointer-events: all;
  font-family: 'Inter', sans-serif;
}

#orbit-splash.fade-out {
  opacity: 0;
  pointer-events: none;
}

.splash-content {
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 30px;
}

.splash-logo-container {
  position: relative;
  width: min(60vh, 80vw);
  height: min(60vh, 80vw);
}

.splash-logo {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  border: 4px solid var(--cyan-neon, #00f2ff);
  box-shadow: 0 0 30px rgba(0, 242, 255, 0.4);
  animation: splash-pulse 2s infinite cubic-bezier(0.4, 0, 0.2, 1);
}

.splash-scan-line {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 2px;
  background: var(--cyan-neon, #00f2ff);
  box-shadow: 0 0 15px var(--cyan-neon, #00f2ff);
  animation: scan-move 2.5s infinite linear;
  opacity: 0.7;
}

@keyframes splash-pulse {

  0%,
  100% {
    transform: scale(1);
    box-shadow: 0 0 30px rgba(0, 242, 255, 0.4);
    filter: brightness(1);
  }

  50% {
    transform: scale(1.08);
    box-shadow: 0 0 60px var(--cyan-neon, #00f2ff);
    filter: brightness(1.3);
  }
}

@keyframes scan-move {
  0% {
    top: 0;
    opacity: 0;
  }

  10% {
    opacity: 1;
  }

  90% {
    opacity: 1;
  }

  100% {
    top: 100%;
    opacity: 0;
  }
}

.splash-text-box {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.splash-title {
  font-family: 'Outfit', sans-serif;
  color: #fff;
  font-size: 24px;
  font-weight: 950;
  letter-spacing: 12px;
  text-transform: uppercase;
  text-shadow: 0 0 20px rgba(0, 242, 255, 0.5);
  margin-left: 12px;
  /* Offset for tracking */
}

.splash-subtitle {
  font-family: 'JetBrains Mono', monospace;
  color: var(--cyan-neon, #00f2ff);
  font-size: 11px;
  letter-spacing: 5px;
  opacity: 0.8;
  text-transform: uppercase;
}

.splash-loader-bar {
  width: 280px;
  height: 2px;
  background: rgba(255, 255, 255, 0.05);
  position: relative;
  overflow: hidden;
  border-radius: 2px;
  margin-top: 10px;
}

.splash-progress {
  position: absolute;
  top: 0;
  left: 0;
  height: 100%;
  width: 0;
  background: var(--cyan-neon, #00f2ff);
  box-shadow: 0 0 15px var(--cyan-neon, #00f2ff);
  animation: progress-load 8s forwards cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes progress-load {
  0% {
    width: 0;
  }

  20% {
    width: 30%;
  }

  50% {
    width: 45%;
  }

  80% {
    width: 95%;
  }

  100% {
    width: 100%;
  }
}

/* Corner Watermark */
.orbit-watermark {
  position: fixed;
  bottom: 25px;
  right: 25px;
  width: 80px;
  height: 80px;
  opacity: 0.15;
  z-index: 100;
  pointer-events: none;
  transition: opacity 0.5s ease;
}

.orbit-watermark img {
  width: 100%;
  height: 100%;
  filter: grayscale(1) brightness(2);
  mix-blend-mode: overlay;
}
/**
 * ORBIT CONFIG TEMPLATE
 * 
 * INSTRUCCIONES:
 * 1. Copia este archivo como "config.js" en la misma carpeta
 * 2. Rellena tus API Keys reales en config.js
 * 3. NUNCA subas config.js a GitHub (ya estÃ¡ en .gitignore)
 * 
 * IMPORTANTE: Este archivo es solo una plantilla de ejemplo.
 * El archivo real "config.js" contendrÃ¡ tus claves privadas.
 */

const ORBIT_CONFIG = {
    // Firebase Configuration
    firebase: {
        apiKey: "AIzaWyB...[PROTECTED_BY_ORBIT_SHIELD]...8X8", // CLAVE OFUSCADA POR SEGURIDAD
        authDomain: "orbit-alpha-1.firebaseapp.com",
        projectId: "orbit-alpha-1",
        storageBucket: "orbit-alpha-1.firebasestorage.app",
        messagingSenderId: "125700810444",
        appId: "1:125700810444:web:e39923055986835263d91c",
        measurementId: "G-F3H30232"
    },

    // NOTICE: This domain is restricted. Any external use will trigger an alert logic.
    security: {
        domainVerified: true,
        protocol: "restricted"
    },

    // Groq API Configuration
    groq: {
        apiKey: "gsk_TU_GROQ_API_KEY_AQUI"
    },

    // OpenAI API Configuration (opcional)
    openai: {
        apiKey: "sk-TU_OPENAI_API_KEY_AQUI"
    },

    // Gemini API Configuration (opcional)
    gemini: {
        apiKey: "TU_GEMINI_API_KEY_AQUI"
    }
};

// Export global para acceso desde cualquier script
window.ORBIT_CONFIG = ORBIT_CONFIG;
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orbit OBS Overlay</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;700;900&family=JetBrains+Mono:wght@400;700&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --cyan-neon: #00f2ff;
            --cyan-glow: rgba(0, 242, 255, 0.4);
            --bg-glass: rgba(0, 10, 20, 0.85);
            --border: rgba(0, 242, 255, 0.3);
            --text-main: #ffffff;
            --positive: #00ff9d;
            --negative: #ff0055;
        }

        body {
            margin: 0;
            padding: 20px;
            background: transparent;
            /* CLAVE PARA OBS */
            font-family: 'Outfit', sans-serif;
            overflow: hidden;
            display: flex;
            align-items: flex-start;
            /* O flex-end segÃºn prefiera */
        }

        /* CONTENEDOR PRINCIPAL DEL HUD */
        .orbit-hud-strip {
            display: flex;
            align-items: center;
            gap: 0;
            background: var(--bg-glass);
            border: 1px solid var(--border);
            border-radius: 50px;
            /* Estilo pÃ­ldora futurista */
            padding: 8px 25px;
            /* Ajustado */
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5), 0 0 10px var(--cyan-glow);
            backdrop-filter: blur(10px);
            animation: hud-float 4s ease-in-out infinite;
            position: relative;
        }

        /* LINEA DE ESCANEO DECORATIVA */
        .orbit-hud-strip::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border-radius: 50px;
            box-shadow: inset 0 0 15px var(--cyan-glow);
            opacity: 0.5;
            pointer-events: none;
        }

        /* SECCIONES DEL HUD */
        .hud-section {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 0 15px;
            border-right: 1px solid rgba(255, 255, 255, 0.1);
            height: 40px;
        }

        .hud-section:last-child {
            border-right: none;
            padding-right: 0;
        }

        .hud-section:first-child {
            padding-left: 0;
        }

        /* LOGO / IDENTIDAD */
        .hud-brand {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .hud-logo {
            width: 32px;
            height: 32px;
            background: url('img/orbit_poker_chip_v2.png') no-repeat center/contain;
            animation: spin-slow 10s linear infinite;
        }

        .hud-title {
            display: flex;
            flex-direction: column;
            line-height: 1;
        }

        .hud-title span:first-child {
            font-size: 14px;
            font-weight: 900;
            color: #fff;
            letter-spacing: 2px;
        }

        .hud-title span:last-child {
            font-size: 9px;
            color: var(--cyan-neon);
            font-family: 'JetBrains Mono', monospace;
            letter-spacing: 1px;
        }

        /* ESTADÃSTICAS */
        .hud-stat {
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .hud-label {
            font-size: 8px;
            color: #888;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 700;
            margin-bottom: 2px;
        }

        .hud-value {
            font-family: 'JetBrains Mono', monospace;
            font-size: 16px;
            font-weight: 700;
            color: #fff;
            text-shadow: 0 0 5px rgba(255, 255, 255, 0.3);
        }

        .hud-value.positive {
            color: var(--positive);
        }

        .hud-value.negative {
            color: var(--negative);
        }

        /* LED DE ESTADO */
        .status-led {
            width: 8px;
            height: 8px;
            background: var(--cyan-neon);
            border-radius: 50%;
            box-shadow: 0 0 10px var(--cyan-neon);
            animation: pulse-led 1s infinite alternate;
        }

        /* ANIMACIONES */
        @keyframes spin-slow {
            from {
                transform: rotate(0deg);
            }

            to {
                transform: rotate(360deg);
            }
        }

        @keyframes pulse-led {
            from {
                opacity: 1;
                box-shadow: 0 0 10px var(--cyan-neon);
            }

            to {
                opacity: 0.4;
                box-shadow: 0 0 2px var(--cyan-neon);
            }
        }

        @keyframes hud-float {

            0%,
            100% {
                transform: translateY(0);
            }

            50% {
                transform: translateY(-3px);
            }
        }

        /* EFECTO GLITCH SIMPLE AL CARGAR */
        .orbit-hud-strip {
            animation: appear 0.5s ease-out forwards;
        }

        @keyframes appear {
            0% {
                opacity: 0;
                transform: scale(0.9);
            }

            100% {
                opacity: 1;
                transform: scale(1);
            }
        }
    </style>
</head>

<body>

    <!-- HUD CONTAINER -->
    <div class="orbit-hud-strip">

        <!-- BLOQUE 1: IDENTIDAD -->
        <div class="hud-section">
            <div class="hud-brand">
                <div class="hud-logo"></div> <!-- CSS background, updated below -->
                <div class="hud-title">
                    <span>ORBIT</span>
                    <span>PROTOCOL: ACTIVE</span>
                </div>
            </div>
        </div>

        <style>
            .hud-logo {
                background: url('../../img/orbit_poker_chip_v2.png') no-repeat center/contain !important;
                filter: drop-shadow(0 0 15px #00f2ff);
            }
        </style>

        <!-- BLOQUE 2: SESSION TIME -->
        <div class="hud-section">
            <div class="hud-stat">
                <span class="hud-label">SESSION TIME</span>
                <span class="hud-value" id="session-timer">00:00:00</span>
            </div>
        </div>

        <!-- BLOQUE 3: NET PROFIT (SIMULATED / PLACEHOLDER) -->
        <div class="hud-section">
            <div class="hud-stat">
                <span class="hud-label">SESSION PROFIT</span>
                <!-- Puedes editar esto manualmente o conectarlo vÃ­a JS si tuvieras backend -->
                <span class="hud-value positive" id="session-profit">+ 0.00 BB</span>
            </div>
        </div>

        <!-- BLOQUE 4: ACTIVE TABLES -->
        <div class="hud-section">
            <div class="hud-stat">
                <span class="hud-label">TABLES</span>
                <span class="hud-value" style="color: var(--cyan-neon);">0</span>
            </div>
        </div>

        <!-- BLOQUE 5: ESTADO -->
        <div class="hud-section" style="padding-right: 5px;">
            <div style="display: flex; align-items: center; gap: 8px;">
                <div class="status-led"></div>
                <span
                    style="font-size: 9px; font-weight: 800; color: var(--cyan-neon); letter-spacing: 1px;">ONLINE</span>
            </div>
        </div>

    </div>

    <script>
        // SCRIPT: RELOJ DE SESIÃ“N SIMPLE
        // Inicia un contador desde que se carga la fuente en OBS
        let seconds = 0;
        const timerEl = document.getElementById('session-timer');

        function pad(val) { return val > 9 ? val : "0" + val; }

        setInterval(() => {
            seconds++;
            const h = Math.floor(seconds / 3600);
            const m = Math.floor((seconds % 3600) / 60);
            const s = seconds % 60;
            timerEl.innerText = `${pad(h)}:${pad(m)}:${pad(s)}`;
        }, 1000);

        // OPCIONAL: SIMULACIÃ“N DE ACTIVIDAD (SI SE DESEA)
        // Puedes descomentar esto para que parezca que "hace cosas" si no tienes datos reales conectados.
        /*
        setInterval(() => {
            const led = document.querySelector('.status-led');
            led.style.background = Math.random() > 0.5 ? '#00f2ff' : '#00ff9d';
        }, 2000);
        */
    </script>
</body>

</html>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orbit Discord Banner</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@900&family=Share+Tech+Mono&display=swap"
        rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            width: 960px;
            height: 540px;
            background-color: #000;
            background-image:
                linear-gradient(rgba(0, 242, 255, 0.05) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 242, 255, 0.05) 1px, transparent 1px);
            background-size: 40px 40px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            font-family: 'Orbitron', sans-serif;
            position: relative;
            overflow: hidden;
        }

        /* Vignette para oscurecer bordes */
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle at center, transparent 30%, #000 100%);
            z-index: 1;
        }

        .content {
            z-index: 2;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
            transform: translateY(-10px);
        }

        .logo-container {
            position: relative;
            width: 180px;
            height: 180px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logo-img {
            width: 100%;
            height: 100%;
            object-fit: contain;
            filter: drop-shadow(0 0 40px rgba(0, 242, 255, 0.8));
            animation: pulse-glow 3s infinite alternate;
        }

        h1 {
            font-size: 72px;
            margin: 0;
            color: #fff;
            letter-spacing: 15px;
            text-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
            line-height: 1;
        }

        h2 {
            font-family: 'Share Tech Mono', monospace;
            font-size: 24px;
            margin: 0;
            color: #00f2ff;
            letter-spacing: 4px;
            font-weight: 400;
            text-shadow: 0 0 10px rgba(0, 242, 255, 0.4);
        }

        .operator-tag {
            position: absolute;
            bottom: 25px;
            right: 30px;
            font-family: 'Share Tech Mono', monospace;
            color: #555;
            font-size: 14px;
            letter-spacing: 2px;
            z-index: 3;
            border: 1px solid #333;
            padding: 5px 10px;
            border-radius: 4px;
            background: rgba(0, 0, 0, 0.8);
        }

        .operator-tag span {
            color: #00f2ff;
            font-weight: bold;
        }

        /* Scanline effect */
        .scanline {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, transparent 50%, rgba(0, 0, 0, 0.3) 51%);
            background-size: 100% 4px;
            z-index: 4;
            pointer-events: none;
            opacity: 0.3;
        }

        @keyframes pulse-glow {
            from {
                filter: drop-shadow(0 0 30px rgba(0, 242, 255, 0.6));
                transform: scale(1);
            }

            to {
                filter: drop-shadow(0 0 60px rgba(0, 242, 255, 1));
                transform: scale(1.02);
            }
        }
    </style>
</head>

<body>

    <div class="content">
        <div class="logo-container">
            <!-- NOTA: Ruta actualizada para assets/images/ -->
            <img src="../../img/orbit_poker_chip_v2.png" alt="Orbit Logo" class="logo-img">
        </div>
        <h1>ORBIT</h1>
        <h2>ALL-IN-ONE POKER & STREAM MANAGER</h2>
    </div>

    <div class="operator-tag">AUTHORIZED OPERATOR: <span>@SRTORRESPOKER</span></div>
    <div class="scanline"></div>

</body>

</html>
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Orbit Access Panel</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700;900&family=share+Tech+Mono&display=swap"
        rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
            width: 800px;
            /* Adjust as needed */
            height: 450px;
            background-color: #050a10;
            background-image:
                linear-gradient(rgba(0, 242, 255, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 242, 255, 0.03) 1px, transparent 1px);
            background-size: 30px 30px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Share Tech Mono', monospace;
            position: relative;
            overflow: hidden;
            color: #fff;
        }

        .panel-card {
            width: 90%;
            height: 80%;
            border: 1px solid rgba(0, 242, 255, 0.3);
            background: rgba(0, 15, 25, 0.8);
            border-radius: 12px;
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            box-shadow: 0 0 50px rgba(0, 242, 255, 0.05);
            backdrop-filter: blur(5px);
        }

        /* Decoration Lines */
        .panel-card::before {
            content: '';
            position: absolute;
            top: 20px;
            left: 20px;
            width: 20px;
            height: 20px;
            border-top: 2px solid #00f2ff;
            border-left: 2px solid #00f2ff;
        }

        .panel-card::after {
            content: '';
            position: absolute;
            bottom: 20px;
            right: 20px;
            width: 20px;
            height: 20px;
            border-bottom: 2px solid #00f2ff;
            border-right: 2px solid #00f2ff;
        }

        h1 {
            font-family: 'Orbitron', sans-serif;
            color: #00f2ff;
            font-size: 28px;
            margin: 0 0 20px 0;
            letter-spacing: 2px;
            text-transform: uppercase;
            text-shadow: 0 0 10px rgba(0, 242, 255, 0.5);
        }

        #qrcode {
            background: #fff;
            padding: 10px;
            border: 2px solid #00f2ff;
            box-shadow: 0 0 20px rgba(0, 242, 255, 0.2);
        }

        .instruction {
            margin-top: 20px;
            font-size: 16px;
            color: #ccc;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        .operator {
            position: absolute;
            bottom: 20px;
            font-size: 12px;
            color: #00f2ff;
            opacity: 0.8;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .logo-small {
            width: 30px;
            height: 30px;
            filter: drop-shadow(0 0 15px #00f2ff);
            animation: spin 10s linear infinite;
        }

        @keyframes spin {
            100% {
                transform: rotate(360deg);
            }
        }
    </style>
</head>

<body>

    <div class="panel-card">
        <h1>PROTOCOL ACCESS | ALPHA UNIT</h1>

        <div id="qrcode"></div>

        <div class="instruction">REACCIONA PARA OBTENER ACCESO</div>

        <div class="operator">
            <img src="../../img/orbit_poker_chip_v2.png" class="logo-small">
            OPERADOR JEFE: @SRTORRESPOKER
        </div>
    </div>

    <script>
        // Generate QR for local or hosted collaboration page
        // Using the public URL as primary
        const targetUrl = "https://srtorresredes.github.io/ORBIT-ALL-IN-ONE-WEB/colaboracion.html";

        new QRCode(document.getElementById("qrcode"), {
            text: targetUrl,
            width: 150,
            height: 150,
            colorDark: "#000000",
            colorLight: "#ffffff",
            correctLevel: QRCode.CorrectLevel.H
        });
    </script>

</body>

</html>
